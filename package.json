{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "validate:api-codegen": "bash -c 'if [ -f artifacts/api-spec/package.json ]; then pnpm --filter @workspace/api-spec run check-generated; else echo \"No artifacts/api-spec workspace present; skipping generated API client validation.\"; fi'",
    "validate:api-compatibility": "bash -c 'if [ -f artifacts/api-spec/package.json ]; then node lib/api-spec/scripts/check-contract-compatibility.mjs; else echo \"No artifacts/api-spec workspace present; skipping API contract compatibility validation.\"; fi'",
    "validate:native-large-text-evidence": "bash -c 'if [ -f scripts/check-native-large-text-evidence.sh ]; then bash scripts/check-native-large-text-evidence.sh; else echo \"No native large text evidence script present; skipping.\"; fi'",
    "validate:mockup-clean": "pnpm --filter @workspace/mockup-sandbox run typecheck",
    "test:unit": "pnpm -r --if-present run test"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "image-size": "link:vendor/image-size-secure",
    "yaml": "^2.9.0"
  }
}
