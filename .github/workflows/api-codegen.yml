name: API generated clients

on:
  push:
    branches: ["development"]
  pull_request:
    branches: ["development"]
    types: [opened, synchronize, reopened, edited]

jobs:
  check-generated:
    name: Check generated API clients
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        id: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        id: setup-pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.26.1

      - name: Set up Node.js
        id: setup-node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install workspace dependencies
        id: install-dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify generated API clients
        run: pnpm validate:api-codegen

      # Breaking contract changes fail this step unless the pull request
      # description declares both pieces of review evidence. Values may be inline:
      #   API_BREAKING_CHANGE_JUSTIFICATION: <why the existing contract must break>
      #   API_BREAKING_CHANGE_MIGRATION_PLAN: <how existing consumers move to the new contract>
      # or an indented/bulleted block directly below either marker, ending at the
      # next blank line or marker. An override that omits either value is rejected.
      # The decision (compatible,
      # blocked, or approved override with its review evidence) is also published
      # to the job summary through GITHUB_STEP_SUMMARY.
      - name: Check API contract compatibility
        if: >-
          ${{
            always() &&
            steps.checkout.outcome == 'success' &&
            steps.setup-pnpm.outcome == 'success' &&
            steps.setup-node.outcome == 'success' &&
            steps.install-dependencies.outcome == 'success'
          }}
        env:
          API_BREAKING_CHANGE_PR_BODY: ${{ github.event.pull_request.body }}
        run: pnpm validate:api-compatibility
