name: Mobile release accessibility gate

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish the tested candidate to both stores after the gate
        required: true
        default: false
        type: boolean
      android_runner_health_only:
        description: Check the Android release runner without starting release jobs
        required: true
        default: false
        type: boolean
      ios_review_candidate_build_id:
        description: Approved iOS candidate build ID (must match the tested build)
        required: false
        type: string
      android_review_candidate_build_id:
        description: Approved Android candidate build ID (must match the tested build)
        required: false
        type: string
      node_range_override:
        description: Optional Node range fixture for validating the release guard
        required: false
        type: string
  push:
    tags:
      - "mobile-v*"
  pull_request:
  workflow_call:
    inputs:
      native_smoke_ios_build_id:
        description: Non-secret EAS build ID of the iOS candidate
        required: true
        type: string
      native_smoke_android_build_id:
        description: Non-secret EAS build ID of the Android candidate
        required: true
        type: string
      node_range_override:
        description: Optional Node range fixture for validating the release guard
        required: false
        type: string
      android_runner_health_only:
        description: Check the Android release runner without starting release jobs
        required: false
        default: false
        type: boolean
    secrets:
      GITHUB_WORKFLOW_PULL_TOKEN_FINAL:
        required: true
      EAS_TOKEN:
        required: false
      SENTRY_AUTH_TOKEN:
        required: false
      NATIVE_SMOKE_IOS_APP_ID:
        required: false
      NATIVE_SMOKE_ANDROID_APP_ID:
        required: false
      NATIVE_SMOKE_EMAIL:
        required: false
      NATIVE_SMOKE_PASSWORD:
        required: false
      NATIVE_SMOKE_DISPLAY_NAME:
        required: false
      NATIVE_SMOKE_IOS_SENTRY_RELEASE:
        required: false
      NATIVE_SMOKE_IOS_SENTRY_DIST:
        required: false
      NATIVE_SMOKE_ANDROID_SENTRY_RELEASE:
        required: false
      NATIVE_SMOKE_ANDROID_SENTRY_DIST:
        required: false
      E2E_CHAT_URL:
        required: false
      E2E_API_URL:
        required: false
      CLERK_PUBLISHABLE_KEY:
        required: false
      CLERK_SECRET_KEY:
        required: false
      DATABASE_URL:
        required: false

permissions:
  actions: read
  contents: read

concurrency:
  group: mobile-release-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    shell: bash

env:
  # Keep every release job on the same concrete Node.js line and let the guard
  # plus contract tests catch any drift from package.json engines.node.
  RELEASE_NODE_VERSION: 24
  PNPM_VERSION: 10.26.1
  EAS_CLI_VERSION: 23.2.0
  NATIVE_SMOKE_IOS_BUILD_ID: ${{ inputs.native_smoke_ios_build_id || vars.NATIVE_SMOKE_IOS_BUILD_ID }}
  NATIVE_SMOKE_ANDROID_BUILD_ID: ${{ inputs.native_smoke_android_build_id || vars.NATIVE_SMOKE_ANDROID_BUILD_ID }}

jobs:
  mobile-release-node-range:
    name: Validate mobile release Node range
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Read package.json Node range
        id: read-node-range
        env:
          NODE_RANGE_OVERRIDE: ${{ inputs.node_range_override }}
        run: |
          set -euo pipefail
          node_range="${NODE_RANGE_OVERRIDE:-}"
          if [[ -z "$node_range" ]]; then
            node_range="$(
              node --input-type=module -e '
              import { readFileSync } from "node:fs";

              try {
                const packageJson = JSON.parse(readFileSync("package.json", "utf8"));
                const configuredRange = packageJson?.engines?.node;
                if (typeof configuredRange === "string" && configuredRange.trim() !== "") {
                  process.stdout.write(configuredRange.trim());
                }
              } catch {
                // The validation step below reports malformed package.json as an invalid range.
              }
              '
            )"
          fi

          if [[ -z "$node_range" ]]; then
            node_range="missing or non-string value"
          fi
          {
            echo "node_range<<NODE_RANGE"
            printf '%s\n' "$node_range"
            echo "NODE_RANGE"
          } >> "$GITHUB_OUTPUT"
          echo "Validating package.json engines.node range: $(printf '%q' "$node_range")"

      - name: Resolve configured Node range
        id: resolve-node-range
        continue-on-error: true
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          # Keep this resolver as the shared range contract. The local contract
          # test pins semver to setup-node's tool-cache matcher (6.3.1).
          node-version: ${{ steps.read-node-range.outputs.node_range }}

      - name: Reject invalid Node range before release checks
        if: ${{ always() }}
        env:
          NODE_RANGE: ${{ steps.read-node-range.outputs.node_range }}
          RESOLVE_RESULT: ${{ steps.resolve-node-range.outcome }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"
          if [[ "$RESOLVE_RESULT" != "success" ]]; then
            safe_node_range="$(sanitize_workflow_text "$NODE_RANGE")"
            if ((${#safe_node_range} > 256)); then
              safe_node_range="${safe_node_range:0:253}..."
            fi
            annotation_node_range="${safe_node_range//%/%25}"
            annotation_node_range="${annotation_node_range//$'\r'/%0D}"
            annotation_node_range="${annotation_node_range//$'\n'/%0A}"
            printf '::error title=Invalid Node range::package.json engines.node: %s\n' "$annotation_node_range"
            {
              echo "## Mobile release Node range"
              echo
              echo "- Status: **FAIL**"
              echo "- Failure: release checks were not started."
              echo "- package.json engines.node: \`$safe_node_range\`"
              echo "- The configured Node range is unsupported; provide a valid semver range."
            } >> "$GITHUB_STEP_SUMMARY"
            echo "Mobile release blocked before release checks: package.json engines.node contains an unsupported range: $safe_node_range" >&2
            exit 1
          fi
          echo "Mobile release Node range is valid: $(sanitize_workflow_text "$NODE_RANGE")"

  android-release-runner-health:
    name: Android release runner health
    if: ${{ always() && github.event_name != 'pull_request' && (inputs.android_runner_health_only == true || (needs.mobile-release-node-range.result == 'success' && needs.mobile-release-credentials.result == 'success')) }}
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
    permissions:
      actions: read
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Check Android release runner labels and status
        env:
          GH_TOKEN: ${{ secrets.GITHUB_WORKFLOW_PULL_TOKEN_FINAL }}
        run: bash scripts/check-android-release-runner-health.sh

  mobile-release-credentials:
    name: Validate mobile release credentials
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    needs:
      - mobile-release-node-range
    runs-on: ubuntu-latest
    environment:
      name: mobile-release
    steps:
      - name: Report every missing required release credential
        env:
          GITHUB_WORKFLOW_PULL_TOKEN_FINAL: ${{ secrets.GITHUB_WORKFLOW_PULL_TOKEN_FINAL }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          NATIVE_SMOKE_IOS_APP_ID: ${{ secrets.NATIVE_SMOKE_IOS_APP_ID }}
          NATIVE_SMOKE_ANDROID_APP_ID: ${{ secrets.NATIVE_SMOKE_ANDROID_APP_ID }}
          NATIVE_SMOKE_EMAIL: ${{ secrets.NATIVE_SMOKE_EMAIL }}
          NATIVE_SMOKE_PASSWORD: ${{ secrets.NATIVE_SMOKE_PASSWORD }}
          NATIVE_SMOKE_IOS_SENTRY_RELEASE: ${{ secrets.NATIVE_SMOKE_IOS_SENTRY_RELEASE }}
          NATIVE_SMOKE_IOS_SENTRY_DIST: ${{ secrets.NATIVE_SMOKE_IOS_SENTRY_DIST }}
          NATIVE_SMOKE_ANDROID_SENTRY_RELEASE: ${{ secrets.NATIVE_SMOKE_ANDROID_SENTRY_RELEASE }}
          NATIVE_SMOKE_ANDROID_SENTRY_DIST: ${{ secrets.NATIVE_SMOKE_ANDROID_SENTRY_DIST }}
          E2E_CHAT_URL: ${{ secrets.E2E_CHAT_URL }}
          E2E_API_URL: ${{ secrets.E2E_API_URL }}
          CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          set -euo pipefail
          required_credentials=(
            GITHUB_WORKFLOW_PULL_TOKEN_FINAL
            SENTRY_AUTH_TOKEN
            NATIVE_SMOKE_IOS_APP_ID
            NATIVE_SMOKE_ANDROID_APP_ID
            NATIVE_SMOKE_EMAIL
            NATIVE_SMOKE_PASSWORD
            NATIVE_SMOKE_IOS_SENTRY_RELEASE
            NATIVE_SMOKE_IOS_SENTRY_DIST
            NATIVE_SMOKE_ANDROID_SENTRY_RELEASE
            NATIVE_SMOKE_ANDROID_SENTRY_DIST
            E2E_CHAT_URL
            E2E_API_URL
            CLERK_PUBLISHABLE_KEY
            CLERK_SECRET_KEY
            DATABASE_URL
          )
          missing_credentials=()
          for credential in "${required_credentials[@]}"; do
            if [[ -z "${!credential:-}" ]]; then
              missing_credentials+=("$credential")
            fi
          done

          if ((${#missing_credentials[@]} > 0)); then
            echo "Mobile release setup is missing required credential keys:" >&2
            printf -- '- %s\n' "${missing_credentials[@]}" >&2
            echo "Add every listed key to the mobile-release environment, then retry." >&2
            exit 1
          fi

          echo "All required mobile release credential keys are configured."

  android-preview-evidence:
    # Keep this display name synchronized with the required check in the repository ruleset.
    name: Android preview evidence
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out handoff record
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install screenshot OCR runtime
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y tesseract-ocr tesseract-ocr-eng
          command -v tesseract >/dev/null
          tesseract --list-langs 2>/dev/null | grep -Fxq "eng"

      - name: Validate changed Android preview records
        env:
          ANDROID_PREVIEW_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          ANDROID_PREVIEW_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          summary_header="## Android preview evidence"
          diff_output=""
          if diff_output="$(
            git diff \
              --name-only \
              --find-renames \
              --diff-filter=ACDMRT \
              "${ANDROID_PREVIEW_BASE_SHA}...${ANDROID_PREVIEW_HEAD_SHA}" \
              -- \
              "artifacts/chat-app/test-results/encrypted-room-recovery/android/**/validation-record.md" \
              "artifacts/chat-app/test-results/encrypted-room-recovery/android/**/android-preview-preflight.json"
          )"; then
            :
          else
            {
              echo "$summary_header"
              echo
              echo "- Status: **FAIL**"
              echo "- Missing-boundary reason: could not locate the pull request's changed Android preview evidence files."
            } >> "$GITHUB_STEP_SUMMARY"
            echo "Could not locate the pull request's changed Android preview evidence files." >&2
            exit 1
          fi

          changed_record_candidates=()
          changed_preflight_paths=()
          while IFS= read -r changed_path; do
            [[ -n "$changed_path" ]] || continue
            case "$changed_path" in
              */validation-record.md)
                changed_record_candidates+=("$changed_path")
                ;;
              */android-preview-preflight.json)
                record_path="${changed_path%/android-preview-preflight.json}/validation-record.md"
                changed_record_candidates+=("$record_path")
                changed_preflight_paths+=("$changed_path")
                ;;
            esac
          done < <(printf '%s\n' "$diff_output")

          if ((${#changed_record_candidates[@]} > 0)); then
            mapfile -t changed_records < <(printf '%s\n' "${changed_record_candidates[@]}" | LC_ALL=C sort -u)
          else
            changed_records=()
          fi

          if ((${#changed_records[@]} == 0)); then
            {
              echo "$summary_header"
              echo
              echo "- Status: **SKIP**"
              echo "- Changed records checked: **0**"
              echo "- Validation: **Not applicable; this pull request changed no Android preview validation record or preflight artifact.**"
            } >> "$GITHUB_STEP_SUMMARY"
            echo "No Android preview validation records or preflight artifacts changed; nothing to validate."
            exit 0
          fi

          {
            echo "$summary_header"
            echo
            echo "- Changed records checked: **${#changed_records[@]}**"
          } >> "$GITHUB_STEP_SUMMARY"

          validator_directory="artifacts/chat-app/scripts"
          validator_file="validate-preview-startup.mjs"
          validator_path="${validator_directory}/${validator_file}"
          validator_failure_reason=""
          if [[ ! -f "$validator_path" ]]; then
            validator_failure_reason="- The Android preview evidence check is missing its delegated validator dependency boundary: ${validator_path} is not present in the checked-out commit. Restore that validator before changing the evidence record."
          fi

          overall_status=0
          for record_path in "${changed_records[@]}"; do
            record_revision="$GITHUB_SHA"
            if [[ ! -f "$record_path" ]]; then
              record_revision="$ANDROID_PREVIEW_BASE_SHA"
            fi
            record_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/blob/${record_revision}/${record_path}"
            record_result=""
            validation_output=""
            screenshot_inspection_status=""
            validation_status=0
            reasons=""
            preflight_path=""
            for changed_preflight_path in "${changed_preflight_paths[@]}"; do
              if [[ "$changed_preflight_path" == "${record_path%/validation-record.md}/android-preview-preflight.json" ]]; then
                preflight_path="$changed_preflight_path"
                break
              fi
            done

            if [[ ! -f "$record_path" ]]; then
              validation_status=1
              reasons="- The changed Android preview validation record is missing from the checked-out commit."
            elif [[ -n "$validator_failure_reason" ]]; then
              validation_status=1
              reasons="$validator_failure_reason"
            else
              record_result="$(
                sed -nE 's/^\*\*Result:[[:space:]]*(PASS|BLOCKED|FAIL).*/\1/p' "$record_path" |
                  head -n 1
              )"
              checker_args=("$record_path")
              if [[ -n "$preflight_path" ]]; then
                checker_args+=("$preflight_path")
              fi
              if validation_output="$(bash scripts/run-untrusted-checker.sh pnpm run validate:android-preview-evidence -- "${checker_args[@]}" 2>&1)"; then
                :
              else
                validation_status=$?
                reasons="$(printf '%s\n' "$validation_output" | sed -n '/^- /p')"
              fi
              screenshot_inspection_status="$(
                printf '%s\n' "$validation_output" |
                  sed -nE 's/^Screenshot inspection status: (.*)$/\1/p' |
                  tail -n 1
              )"
            fi

            if ((validation_status != 0)); then
              overall_status=1
              if [[ -z "$reasons" ]]; then
                reasons="- The Android preview evidence record did not satisfy the validation contract."
              fi
            fi
            safe_record_path="$(sanitize_workflow_text "$record_path")"
            safe_record_url="$(sanitize_workflow_text "$record_url")"
            safe_reasons="$(sanitize_workflow_text "$reasons")"
            safe_screenshot_inspection_status="$(sanitize_workflow_text "$screenshot_inspection_status")"

            {
              echo
              echo "### [${safe_record_path}](${safe_record_url})"
              if ((validation_status == 0)); then
                echo "- Validation: **PASS**"
                if [[ "$record_result" == "BLOCKED" ]]; then
                  echo "- Record result: **BLOCKED (valid)**"
                else
                  echo "- Record result: **${record_result:-Unavailable}**"
                fi
              else
                echo "- Validation: **FAIL**"
                echo "- Record result: **${record_result:-Unavailable}**"
                echo
                echo "#### Missing-boundary reason"
                printf '%s\n' "$safe_reasons" | render_markdown_code_block
              fi
              if [[ -n "$safe_screenshot_inspection_status" ]]; then
                echo
                echo "#### Screenshot inspection"
                echo "- Status: \`${safe_screenshot_inspection_status}\`"
                echo "- Review note: **Check the saved screenshot manually before approval; this status contains no image text or matched content.**"
              fi
            } >> "$GITHUB_STEP_SUMMARY"

            if ((validation_status != 0)); then
              printf '%s\n' "$safe_reasons" >&2
            fi
          done

          exit "$overall_status"

  ios-preview-evidence:
    # Keep this display name synchronized with the required check in the repository ruleset.
    name: iOS preview evidence
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out handoff record
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Validate changed iOS preview records
        env:
          IOS_PREVIEW_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          IOS_PREVIEW_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          summary_header="## iOS preview evidence"
          diff_output=""
          if diff_output="$(
            git diff \
              --name-only \
              --find-renames \
              --diff-filter=ACDMRT \
              "${IOS_PREVIEW_BASE_SHA}...${IOS_PREVIEW_HEAD_SHA}" \
              -- \
              "artifacts/chat-app/test-results/encrypted-room-recovery/ios/**/validation-record.md" \
              "artifacts/chat-app/test-results/encrypted-room-recovery/ios/**/ios-preview-preflight.json"
          )"; then
            :
          else
            {
              echo "$summary_header"
              echo
              echo "- Status: **FAIL**"
              echo "- Missing-boundary reason: could not locate the pull request's changed iOS preview records."
            } >> "$GITHUB_STEP_SUMMARY"
            echo "Could not locate the pull request's changed iOS preview records." >&2
            exit 1
          fi

          changed_record_candidates=()
          changed_preflight_paths=()
          while IFS= read -r changed_path; do
            [[ -n "$changed_path" ]] || continue
            case "$changed_path" in
              */validation-record.md)
                changed_record_candidates+=("$changed_path")
                ;;
              */ios-preview-preflight.json)
                record_path="${changed_path%/ios-preview-preflight.json}/validation-record.md"
                changed_record_candidates+=("$record_path")
                changed_preflight_paths+=("$changed_path")
                ;;
            esac
          done < <(printf '%s\n' "$diff_output")

          if ((${#changed_record_candidates[@]} > 0)); then
            mapfile -t changed_records < <(printf '%s\n' "${changed_record_candidates[@]}" | sort -u)
          else
            changed_records=()
          fi

          if ((${#changed_records[@]} == 0)); then
            {
              echo "$summary_header"
              echo
              echo "- Status: **SKIP**"
              echo "- Changed records checked: **0**"
              echo "- Validation: **Not applicable; this pull request changed no iOS preview validation record or preflight artifact.**"
            } >> "$GITHUB_STEP_SUMMARY"
            echo "No iOS preview validation records or preflight artifacts changed; nothing to validate."
            exit 0
          fi

          {
            echo "$summary_header"
            echo
            echo "- Changed records checked: **${#changed_records[@]}**"
          } >> "$GITHUB_STEP_SUMMARY"

          validator_directory="artifacts/chat-app/scripts"
          validator_file="validate-preview-startup.mjs"
          validator_path="${validator_directory}/${validator_file}"
          validator_failure_reason=""
          if [[ ! -f "$validator_path" ]]; then
            validator_failure_reason="- The iOS preview evidence check is missing its delegated validator dependency boundary: ${validator_path} is not present in the checked-out commit. Restore that validator before changing the evidence record."
          fi

          overall_status=0
          for record_path in "${changed_records[@]}"; do
            record_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/blob/${GITHUB_SHA}/${record_path}"
            record_result=""
            validation_output=""
            validation_status=0
            reasons=""
            preflight_path=""
            for changed_preflight_path in "${changed_preflight_paths[@]}"; do
              if [[ "$changed_preflight_path" == "${record_path%/validation-record.md}/ios-preview-preflight.json" ]]; then
                preflight_path="$changed_preflight_path"
                break
              fi
            done

            if [[ ! -f "$record_path" ]]; then
              validation_status=1
              reasons="- The changed iOS preview validation record is missing from the checked-out commit."
            elif [[ -n "$validator_failure_reason" ]]; then
              validation_status=1
              reasons="$validator_failure_reason"
            else
              record_result="$(
                sed -nE 's/^\*\*Result:[[:space:]]*(PASS|BLOCKED|FAIL).*/\1/p' "$record_path" |
                  head -n 1
              )"
              checker_args=("$record_path")
              if [[ -n "$preflight_path" ]]; then
                checker_args+=("$preflight_path")
              fi
              if validation_output="$(bash scripts/run-untrusted-checker.sh pnpm run validate:ios-preview-evidence -- "${checker_args[@]}" 2>&1)"; then
                :
              else
                validation_status=$?
                reasons="$(printf '%s\n' "$validation_output" | sed -n '/^- /p')"
              fi
            fi

            if ((validation_status != 0)); then
              overall_status=1
              if [[ -z "$reasons" ]]; then
                reasons="- The iOS preview evidence record did not satisfy the validation contract."
              fi
            fi
            safe_record_path="$(sanitize_workflow_text "$record_path")"
            safe_record_url="$(sanitize_workflow_text "$record_url")"
            safe_reasons="$(sanitize_workflow_text "$reasons")"

            {
              echo
              echo "### [${safe_record_path}](${safe_record_url})"
              if ((validation_status == 0)); then
                echo "- Validation: **PASS**"
                case "$record_result" in
                  BLOCKED)
                    echo "- Record result: **BLOCKED (valid physical-phone handoff unavailable)**"
                    ;;
                  FAIL)
                    echo "- Record result: **FAIL (public edge)**"
                    ;;
                  *)
                    echo "- Record result: **${record_result:-Unavailable}**"
                    ;;
                esac
              else
                echo "- Validation: **FAIL**"
                echo "- Record result: **${record_result:-Unavailable}**"
                echo
                echo "#### Missing-boundary reason"
                printf '%s\n' "$safe_reasons"
              fi
            } >> "$GITHUB_STEP_SUMMARY"

            if ((validation_status != 0)); then
              printf '%s\n' "$safe_reasons" >&2
            fi
          done

          exit "$overall_status"

  native-evidence-summary-regression:
    name: Native evidence summary regression
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    needs:
      - mobile-release-node-range
    permissions:
      actions: write
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}

      - name: Upload controlled Android artifact for missing iOS scenario
        shell: bash
        run: |
          set -euo pipefail
          fixture_root="$RUNNER_TEMP/native-evidence-summary-download-fixture"
          rm -rf -- "$fixture_root"
          mkdir -p -- "$fixture_root/android"
          printf 'controlled Android artifact\n' > "$fixture_root/android/controlled-marker.txt"

      - name: Store controlled Android artifact for missing iOS scenario
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: native-evidence-summary-controlled-android
          path: ${{ runner.temp }}/native-evidence-summary-download-fixture/android/**
          if-no-files-found: error

      - name: Download controlled missing iOS artifact
        if: ${{ always() }}
        continue-on-error: true
        id: download-controlled-ios
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-evidence-summary-controlled-ios
          path: ${{ runner.temp }}/native-evidence-summary-downloads/ios

      - name: Download controlled Android artifact after iOS failure
        if: ${{ always() }}
        continue-on-error: true
        id: download-controlled-android
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-evidence-summary-controlled-android
          path: ${{ runner.temp }}/native-evidence-summary-downloads/android

      - name: Run checker after controlled artifact outage
        if: ${{ always() }}
        continue-on-error: true
        id: controlled-artifact-check
        env:
          REVIEWED_REF: ${{ github.ref }}
          NATIVE_IOS_EVIDENCE_ARTIFACT_URL: ""
          NATIVE_ANDROID_EVIDENCE_ARTIFACT_URL: ""
          NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.download-controlled-ios.outcome }}
          NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.download-controlled-android.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          controlled_summary_path="$RUNNER_TEMP/native-evidence-summary-controlled.md"
          rm -f -- "$controlled_summary_path"
          resolved_commit_sha="$(git rev-parse --verify HEAD)"

          {
            echo "## Reviewed release revision"
            echo
            printf -- '- Checked ref: `%s`\n' "$REVIEWED_REF"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
            echo
            echo "## Controlled native artifact outage"
            echo "- Scenario: iOS artifact missing; Android artifact available"
          } >> "$GITHUB_STEP_SUMMARY"

          GITHUB_STEP_SUMMARY="$controlled_summary_path" \
            bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh \
            "$RUNNER_TEMP/native-evidence-summary-downloads"

      - name: Confirm controlled outage blocks promotion and preserves both sections
        if: ${{ always() }}
        env:
          CHECK_RESULT: ${{ steps.controlled-artifact-check.outcome }}
          IOS_DOWNLOAD_RESULT: ${{ steps.download-controlled-ios.outcome }}
          ANDROID_DOWNLOAD_RESULT: ${{ steps.download-controlled-android.outcome }}
          CONTROLLED_SUMMARY_PATH: ${{ runner.temp }}/native-evidence-summary-controlled.md
        shell: bash
        run: |
          set -euo pipefail
          if [[ "$IOS_DOWNLOAD_RESULT" != "failure" ]]; then
            echo "The controlled iOS artifact download unexpectedly succeeded." >&2
            exit 1
          fi
          if [[ "$ANDROID_DOWNLOAD_RESULT" != "success" ]]; then
            echo "The Android download did not continue successfully after the iOS failure." >&2
            exit 1
          fi
          if [[ "$CHECK_RESULT" != "failure" ]]; then
            echo "The native evidence checker did not block the controlled outage." >&2
            exit 1
          fi

          ios_section="$(
            awk '
              /^## iOS native large-text evidence$/ { collecting=1 }
              /^## Android native large-text evidence$/ { collecting=0 }
              collecting { print }
            ' "$CONTROLLED_SUMMARY_PATH"
          )"
          android_section="$(
            awk '
              /^## Android native large-text evidence$/ { collecting=1 }
              collecting { print }
            ' "$CONTROLLED_SUMMARY_PATH"
          )"

          require_contains() {
            local section="$1"
            local expected="$2"
            if ! grep -Fq -- "$expected" <<< "$section"; then
              echo "Expected controlled outage summary to contain: $expected" >&2
              exit 1
            fi
          }

          require_not_contains() {
            local section="$1"
            local unexpected="$2"
            if grep -Fq -- "$unexpected" <<< "$section"; then
              echo "Controlled outage summary contained cross-platform detail: $unexpected" >&2
              exit 1
            fi
          }

          require_contains "$ios_section" "- Status: **FAIL**"
          require_contains "$ios_section" "- Artifact download: **FAIL**"
          require_contains "$ios_section" "Rerun the iOS native large-text job"
          require_not_contains "$ios_section" "$RUNNER_TEMP/native-evidence-summary-downloads/android"

          require_contains "$android_section" "- Status: **FAIL**"
          require_contains "$android_section" "- Artifact download: **PASS**"
          require_contains "$android_section" "No timestamped evidence run directory exists"
          require_not_contains "$android_section" "$RUNNER_TEMP/native-evidence-summary-downloads/ios"

          {
            echo
            echo "- Download continuation: **PASS** (Android completed after the iOS failure)"
            echo "- Promotion blocker: **PASS** (checker returned failure)"
            echo "- Recovery: **Rerun the failed native job or make its artifact available, then rerun the mobile release gate.**"
            cat "$CONTROLLED_SUMMARY_PATH"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Verify blocked native evidence summary
        env:
          REVIEWED_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          blocked_root="$(mktemp -d)"
          summary_path="$(mktemp)"
          trap 'rm -rf "$blocked_root" "$summary_path"' EXIT
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          if GITHUB_STEP_SUMMARY="$summary_path" bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "$blocked_root"; then
            echo "The blocked native evidence scenario unexpectedly passed." >&2
            exit 1
          fi

          ios_section="$(
            awk '
              /^## iOS native large-text evidence$/ { collecting=1 }
              /^## Android native large-text evidence$/ { collecting=0 }
              collecting { print }
            ' "$summary_path"
          )"
          android_section="$(
            awk '
              /^## Android native large-text evidence$/ { collecting=1 }
              collecting { print }
            ' "$summary_path"
          )"

          require_contains() {
            local section="$1"
            local expected="$2"
            if ! grep -Fq -- "$expected" <<< "$section"; then
              echo "Expected blocked native evidence summary to contain: $expected" >&2
              exit 1
            fi
          }

          require_not_contains() {
            local section="$1"
            local unexpected="$2"
            if grep -Fq -- "$unexpected" <<< "$section"; then
              echo "Blocked native evidence summary contained cross-platform detail: $unexpected" >&2
              exit 1
            fi
          }

          require_contains "$ios_section" "- Status: **FAIL**"
          require_contains "$ios_section" "- Validated run directory: **Unavailable**"
          require_contains "$ios_section" "- Detailed evidence report: **Unavailable**"
          require_contains "$ios_section" "Missing result directory: $blocked_root/ios"
          require_not_contains "$ios_section" "$blocked_root/android"

          require_contains "$android_section" "- Status: **FAIL**"
          require_contains "$android_section" "- Validated run directory: **Unavailable**"
          require_contains "$android_section" "- Detailed evidence report: **Unavailable**"
          require_contains "$android_section" "Missing result directory: $blocked_root/android"
          require_not_contains "$android_section" "$blocked_root/ios"

          cat "$summary_path" >> "$GITHUB_STEP_SUMMARY"

      - name: Verify failed platform artifact download summary
        env:
          REVIEWED_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          source scripts/native-release-recovery-contract.sh
          failed_download_root="$(mktemp -d)"
          summary_path="$(mktemp)"
          trap 'rm -rf "$failed_download_root" "$summary_path"' EXIT
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          if \
            GITHUB_STEP_SUMMARY="$summary_path" \
            NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT=failure \
            NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT=success \
            NATIVE_ANDROID_EVIDENCE_ARTIFACT_URL="https://github.example/example/chat-app/actions/runs/123/artifacts/456" \
            bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "$failed_download_root"; then
            echo "The failed native evidence download scenario unexpectedly passed." >&2
            exit 1
          fi

          ios_section="$(
            awk '
              /^## iOS native large-text evidence$/ { collecting=1 }
              /^## Android native large-text evidence$/ { collecting=0 }
              collecting { print }
            ' "$summary_path"
          )"
          android_section="$(
            awk '
              /^## Android native large-text evidence$/ { collecting=1 }
              collecting { print }
            ' "$summary_path"
          )"

          require_contains() {
            local section="$1"
            local expected="$2"
            if ! grep -Fq -- "$expected" <<< "$section"; then
              echo "Expected failed-download native evidence summary to contain: $expected" >&2
              exit 1
            fi
          }

          require_not_contains() {
            local section="$1"
            local unexpected="$2"
            if grep -Fq -- "$unexpected" <<< "$section"; then
              echo "Failed-download native evidence summary contained unsafe detail: $unexpected" >&2
              exit 1
            fi
          }

          require_contains "$ios_section" "- Status: **FAIL**"
          require_contains "$ios_section" "- Artifact download: **FAIL**"
          require_contains "$ios_section" "- Validated run directory: **Unavailable**"
          require_contains "$ios_section" "- Detailed evidence report: **Unavailable**"
          require_contains "$ios_section" "native evidence artifact download did not complete"
          require_contains "$ios_section" "$NATIVE_IOS_RECOVERY_LINE"
          require_not_contains "$ios_section" "https://github.example"

          require_contains "$android_section" "- Status: **FAIL**"
          require_contains "$android_section" "- Artifact download: **PASS**"
          require_contains "$android_section" "- Validated run directory: **Unavailable**"
          require_contains "$android_section" "- Detailed evidence report: **Unavailable**"
          require_contains "$android_section" "Missing result directory:"
          require_not_contains "$android_section" "$failed_download_root/ios"

          cat "$summary_path" >> "$GITHUB_STEP_SUMMARY"

      - name: Verify both platform artifact download failures have recovery guidance
        env:
          REVIEWED_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          source scripts/native-release-recovery-contract.sh
          failed_download_root="$(mktemp -d)"
          summary_path="$(mktemp)"
          trap 'rm -rf "$failed_download_root" "$summary_path"' EXIT
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          if \
            GITHUB_STEP_SUMMARY="$summary_path" \
            NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT=failure \
            NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT=failure \
            NATIVE_IOS_EVIDENCE_ARTIFACT_URL="https://github.example/example/chat-app/actions/runs/123/artifacts/456" \
            NATIVE_ANDROID_EVIDENCE_ARTIFACT_URL="https://github.example/example/chat-app/actions/runs/123/artifacts/789" \
            bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "$failed_download_root"; then
            echo "The both-failed native evidence download scenario unexpectedly passed." >&2
            exit 1
          fi

          require_contains() {
            local section="$1"
            local expected="$2"
            if ! grep -Fq -- "$expected" <<< "$section"; then
              echo "Expected both-failed native evidence summary to contain: $expected" >&2
              exit 1
            fi
          }

          require_not_contains() {
            local section="$1"
            local unexpected="$2"
            if grep -Fq -- "$unexpected" <<< "$section"; then
              echo "Both-failed native evidence summary contained unsafe detail: $unexpected" >&2
              exit 1
            fi
          }

          ios_section="$(
            awk '
              /^## iOS native large-text evidence$/ { collecting=1 }
              /^## Android native large-text evidence$/ { collecting=0 }
              collecting { print }
            ' "$summary_path"
          )"
          android_section="$(
            awk '
              /^## Android native large-text evidence$/ { collecting=1 }
              collecting { print }
            ' "$summary_path"
          )"

          require_contains "$ios_section" "- Status: **FAIL**"
          require_contains "$ios_section" "- Artifact download: **FAIL**"
          require_contains "$ios_section" "$NATIVE_IOS_RECOVERY_LINE"
          require_not_contains "$ios_section" "https://github.example"

          require_contains "$android_section" "- Status: **FAIL**"
          require_contains "$android_section" "- Artifact download: **FAIL**"
          require_contains "$android_section" "$NATIVE_ANDROID_RECOVERY_LINE"
          require_not_contains "$android_section" "https://github.example"

          cat "$summary_path" >> "$GITHUB_STEP_SUMMARY"

      - name: Verify temporary native evidence artifact outage recovers on retry
        env:
          REVIEWED_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          recovery_root="$(mktemp -d)"
          pass_summary_path="$(mktemp)"
          failed_retry_summary_path="$(mktemp)"
          empty_retry_summary_path="$(mktemp)"
          attempt_file="$recovery_root/ios-download-attempt"
          empty_attempt_file="$recovery_root/ios-empty-download-attempt"
          recovered_submission_marker="$recovery_root/recovered-submission-command-ran"
          failed_submission_marker="$recovery_root/failed-submission-command-ran"
          empty_retry_submission_marker="$recovery_root/empty-retry-submission-command-ran"
          trap 'rm -rf "$recovery_root" "$pass_summary_path" "$failed_retry_summary_path" "$empty_retry_summary_path"' EXIT
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          write_controlled_evidence() {
            local platform="$1"
            local run_dir="$recovery_root/$platform/20260915T120000Z"
            mkdir -p "$run_dir/screenshots" "$run_dir/call-surface"
            printf 'controlled\n' > "$run_dir/candidate-build-id.txt"
            if [[ "$platform" == "ios" ]]; then
              printf 'platform=ios\ncandidate_build_id=controlled\nrecorded_at_utc=2026-09-15T12:00:00Z\ndevice=controlled-device\n' > "$run_dir/runner-metadata.txt"
            else
              printf 'platform=android\ncandidate_build_id=controlled\nrecorded_at_utc=2026-09-15T12:00:00Z\ndevice_serial=controlled\ndevice_model=controlled\nandroid_release=controlled\nandroid_api=36\nscreen_dp=320x568\ndensity_dpi=160\nuser_rotation=0\n' > "$run_dir/runner-metadata.txt"
            fi
            printf 'platform=%s\nrun_mode=release-gate\ncandidate_build_id=controlled\nstatus=PASS\nrecorded_at_utc=2026-09-15T12:00:00Z\n' "$platform" > "$run_dir/pass-fail-record.txt"
            printf 'platform=%s\nreviewer=controlled-reviewer\nreviewed_at_utc=2026-09-15T12:01:00Z\ncandidate_build_id=controlled\ndecision=APPROVED\napproval_scope=candidate\n' "$platform" > "$run_dir/review-record.txt"
            printf '{}\n' > "$run_dir/native-info.json"
            printf '# Native branding validation\n\n- Status: **PASS**\n' > "$run_dir/native-branding-check.md"
            printf '<testsuite tests="1" failures="0"></testsuite>\n' > "$run_dir/maestro-results.xml"
            printf '<testsuite tests="1" failures="0"></testsuite>\n' > "$run_dir/sentry-maestro-results.xml"
            printf 'platform=%s\ncandidate_build_id=controlled\nmarker=controlled\n' "$platform" > "$run_dir/sentry-trigger.txt"
            printf '{"status":"PASS","eventId":"0123456789abcdef0123456789abcdef","platform":"%s","candidateBuildId":"controlled","marker":"controlled","release":"controlled","dist":"1","readableFrame":{"filename":"controlled.tsx","function":"createNativeSourceMapProbeError","line":1,"column":1},"storageRecovery":{"eventId":"fedcba9876543210fedcba9876543210","message":"Room key persistence retry failed","operation":"save"}}\n' "$platform" > "$run_dir/sentry-source-map-evidence.json"
            for screenshot_index in $(seq 1 11); do
              printf 'controlled screenshot\n' > "$run_dir/screenshots/screen-$screenshot_index.png"
            done
            for call_index in $(seq 1 2); do
              printf 'controlled call surface\n' > "$run_dir/call-surface/call-$call_index.png"
            done
          }

          simulate_store_submission() {
            local marker="$1"
            printf 'submission-reached\n' > "$marker"
          }

          download_ios_once() {
            local attempt=0
            if [[ -s "$attempt_file" ]]; then
              attempt="$(cat "$attempt_file")"
            fi
            attempt=$((attempt + 1))
            printf '%s\n' "$attempt" > "$attempt_file"
            if ((attempt == 1)); then
              echo "Controlled iOS artifact outage injected on the first download attempt." >&2
              return 1
            fi
            write_controlled_evidence ios
          }

          download_ios_empty_once() {
            local attempt=0
            if [[ -s "$empty_attempt_file" ]]; then
              attempt="$(cat "$empty_attempt_file")"
            fi
            attempt=$((attempt + 1))
            printf '%s\n' "$attempt" > "$empty_attempt_file"
            if ((attempt == 1)); then
              echo "Controlled iOS artifact outage injected before the empty retry." >&2
              return 1
            fi
            mkdir -p "$recovery_root/ios"
          }

          set +e
          download_ios_once
          initial_download_status=$?
          set -e
          if [[ "$initial_download_status" == "0" ]]; then
            echo "The controlled iOS first download unexpectedly succeeded." >&2
            exit 1
          fi
          if [[ -e "$recovery_root/ios" ]]; then
            echo "The failed first download left partial iOS evidence behind." >&2
            exit 1
          fi

          set +e
          download_ios_once
          retry_download_status=$?
          set -e
          if [[ "$retry_download_status" != "0" ]]; then
            echo "The controlled iOS retry did not recover the artifact download." >&2
            exit 1
          fi
          if [[ "$(cat "$attempt_file")" != "2" ]]; then
            echo "The controlled iOS artifact was not attempted exactly once before retry." >&2
            exit 1
          fi

          write_controlled_evidence android

          if \
            GITHUB_STEP_SUMMARY="$pass_summary_path" \
            NATIVE_EVIDENCE_REQUIRE_APPROVAL=1 \
            NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT=success \
            NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT=success \
            bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "$recovery_root"; then
            simulate_store_submission "$recovered_submission_marker"
          else
            echo "The strict native evidence checker rejected the artifact after a successful retry." >&2
            exit 1
          fi

          if [[ ! -e "$recovered_submission_marker" ]]; then
            echo "The recovered strict evidence check did not reach the simulated submission boundary." >&2
            exit 1
          fi

          rm -rf -- "$recovery_root/ios"
          set +e
          download_ios_empty_once
          empty_initial_download_status=$?
          set -e
          if [[ "$empty_initial_download_status" == "0" ]]; then
            echo "The controlled iOS empty-artifact first download unexpectedly succeeded." >&2
            exit 1
          fi

          set +e
          download_ios_empty_once
          empty_retry_download_status=$?
          set -e
          if [[ "$empty_retry_download_status" != "0" ]]; then
            echo "The controlled iOS empty-artifact retry did not report success." >&2
            exit 1
          fi
          if [[ ! -d "$recovery_root/ios" ]] ||
            find "$recovery_root/ios" -mindepth 1 -maxdepth 1 -type d -print -quit | grep -q .; then
            echo "The controlled iOS empty-artifact retry unexpectedly produced a timestamped evidence run." >&2
            exit 1
          fi

          if \
            GITHUB_STEP_SUMMARY="$empty_retry_summary_path" \
            NATIVE_EVIDENCE_REQUIRE_APPROVAL=1 \
            NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT=success \
            NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT=success \
            bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "$recovery_root"; then
            simulate_store_submission "$empty_retry_submission_marker"
            echo "The strict native evidence checker approved an empty artifact after a successful retry." >&2
            exit 1
          fi

          if [[ -e "$empty_retry_submission_marker" ]]; then
            echo "The successful empty retry reached the simulated store submission boundary." >&2
            exit 1
          fi
          grep -Fq -- "- Status: **FAIL**" "$empty_retry_summary_path"
          grep -Fq -- "- Artifact download: **PASS**" "$empty_retry_summary_path"
          grep -Fq -- "no timestamped evidence run directory exists" "$empty_retry_summary_path"
          cat "$empty_retry_summary_path" >> "$GITHUB_STEP_SUMMARY"

          write_controlled_evidence ios

          if \
            GITHUB_STEP_SUMMARY="$failed_retry_summary_path" \
            NATIVE_EVIDENCE_REQUIRE_APPROVAL=1 \
            NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT=failure \
            NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT=success \
            bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "$recovery_root"; then
            simulate_store_submission "$failed_submission_marker"
            echo "The native evidence checker passed despite a failed retry." >&2
            exit 1
          fi

          if [[ -e "$failed_submission_marker" ]]; then
            echo "The failed retry reached the simulated store submission boundary." >&2
            exit 1
          fi

          grep -Fq -- "- Status: **PASS**" "$pass_summary_path"
          grep -Fq -- "- Artifact download: **PASS**" "$pass_summary_path"
          grep -Fq -- "- Status: **FAIL**" "$failed_retry_summary_path"
          grep -Fq -- "- Artifact download: **FAIL**" "$failed_retry_summary_path"
          cat "$pass_summary_path" >> "$GITHUB_STEP_SUMMARY"

      - name: Prepare stale iOS evidence before forced cleanup failure
        if: ${{ always() }}
        env:
          STALE_IOS_EVIDENCE_ROOT: ${{ runner.temp }}/native-retry-cleanup-failure/ios
        shell: bash
        run: |
          set -euo pipefail
          rm -rf -- "${STALE_IOS_EVIDENCE_ROOT%/ios}"
          mkdir -p -- "$STALE_IOS_EVIDENCE_ROOT"
          printf 'cleanup-failure-evidence-must-not-appear\n' > "$STALE_IOS_EVIDENCE_ROOT/stale-marker.txt"

      - name: Force iOS retry cleanup failure
        if: ${{ always() }}
        continue-on-error: true
        id: cleanup-forced-ios
        env:
          STALE_IOS_EVIDENCE_ROOT: ${{ runner.temp }}/native-retry-cleanup-failure/ios
        shell: bash
        run: |
          set -euo pipefail
          test -f "$STALE_IOS_EVIDENCE_ROOT/stale-marker.txt"
          echo "Controlled iOS retry cleanup failure injected before the retry." >&2
          false

      - name: Retry iOS native evidence download after failed cleanup
        if: ${{ always() && steps.cleanup-forced-ios.outcome == 'success' }}
        continue-on-error: true
        id: retry-forced-ios
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-retry-cleanup-failure-ios
          path: ${{ runner.temp }}/native-retry-cleanup-failure/ios

      - name: Run checker after forced retry cleanup failure
        if: ${{ always() }}
        continue-on-error: true
        id: forced-cleanup-check
        env:
          REVIEWED_REF: ${{ github.ref }}
          SUMMARY_PATH: ${{ runner.temp }}/native-retry-cleanup-failure/summary.md
          NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.retry-forced-ios.outcome }}
          NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT: failure
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          rm -f -- "$SUMMARY_PATH"
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"
          GITHUB_STEP_SUMMARY="$SUMMARY_PATH" bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh "${SUMMARY_PATH%/summary.md}"

      - name: Verify forced retry cleanup failure remains visible and redacted
        if: ${{ always() }}
        env:
          CHECK_RESULT: ${{ steps.forced-cleanup-check.outcome }}
          CLEANUP_RESULT: ${{ steps.cleanup-forced-ios.outcome }}
          RETRY_RESULT: ${{ steps.retry-forced-ios.outcome }}
          SUMMARY_PATH: ${{ runner.temp }}/native-retry-cleanup-failure/summary.md
          STALE_IOS_EVIDENCE_ROOT: ${{ runner.temp }}/native-retry-cleanup-failure/ios
        shell: bash
        run: |
          set -euo pipefail
          source scripts/native-release-recovery-contract.sh
          if [[ "$CLEANUP_RESULT" != "failure" ]]; then
            echo "The controlled iOS cleanup failure did not fail as expected." >&2
            exit 1
          fi
          if [[ "$RETRY_RESULT" != "skipped" ]]; then
            echo "The iOS retry was not skipped after cleanup failed." >&2
            exit 1
          fi
          if [[ ! -f "$STALE_IOS_EVIDENCE_ROOT/stale-marker.txt" ]]; then
            echo "The controlled stale iOS evidence marker was unexpectedly removed." >&2
            exit 1
          fi
          if find "$STALE_IOS_EVIDENCE_ROOT" -mindepth 1 -maxdepth 1 -type d -print -quit | grep -q .; then
            echo "The skipped iOS retry extracted a timestamped evidence directory." >&2
            exit 1
          fi
          if [[ "$CHECK_RESULT" != "failure" ]]; then
            echo "The native evidence checker did not block after cleanup failure." >&2
            exit 1
          fi
          ios_section="$(
            awk '
              /^## iOS native large-text evidence$/ { collecting=1 }
              /^## Android native large-text evidence$/ { collecting=0 }
              collecting { print }
            ' "$SUMMARY_PATH"
          )"
          grep -Fq -- "- Status: **FAIL**" <<< "$ios_section"
          grep -Fq -- "- Artifact download: **FAIL**" <<< "$ios_section"
          grep -Fq -- "$NATIVE_IOS_RECOVERY_LINE" <<< "$ios_section"
          if grep -Fq -- "cleanup-failure-evidence-must-not-appear" "$SUMMARY_PATH"; then
            echo "The forced cleanup failure summary exposed stale evidence text." >&2
            exit 1
          fi
          cat "$SUMMARY_PATH" >> "$GITHUB_STEP_SUMMARY"

      - name: Run controlled failed mobile publish privacy check
        id: native-evidence-privacy
        continue-on-error: true
        env:
          NATIVE_EVIDENCE_PRIVACY_FAILURE_FIXTURE_ROOT: ${{ runner.temp }}/native-evidence-privacy-publish
        run: bash scripts/run-untrusted-checker.sh pnpm run test:native-large-text-evidence

      - name: Summarize native evidence privacy regression
        if: ${{ always() }}
        env:
          PRIVACY_RESULT: ${{ steps.native-evidence-privacy.outcome }}
        run: |
          set -euo pipefail
          {
            echo "## Native evidence privacy and submission-boundary regression"
            echo
            if [[ "$PRIVACY_RESULT" == "success" ]]; then
              echo "- Status: **PASS**"
              echo "- Result: privacy and submission-boundary checks passed."
            else
              echo "- Status: **BLOCKED**"
              echo "- Result: privacy checks failed; store submission is blocked."
              echo '- Details: Review the "Run native large-text evidence privacy and submission-boundary regression" step log for checker diagnostics.'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Simulate mobile store submission after privacy check
        if: ${{ steps.native-evidence-privacy.outcome == 'success' }}
        env:
          SUBMISSION_MARKER: ${{ runner.temp }}/native-evidence-privacy-publish/store-submission-command-ran
        run: printf 'submission-reached\n' > "$SUBMISSION_MARKER"

      - name: Verify failed privacy summary blocks simulated store submission
        if: ${{ always() }}
        env:
          PRIVACY_RESULT: ${{ steps.native-evidence-privacy.outcome }}
          PRIVACY_CHECKER_SUMMARY: ${{ runner.temp }}/native-evidence-privacy-publish/privacy-checker-summary.md
          SUBMISSION_MARKER: ${{ runner.temp }}/native-evidence-privacy-publish/store-submission-command-ran
        shell: bash
        run: |
          set -euo pipefail
          fixture_sentinel="privacy-fixture-contents-must-not-appear"
          if [[ "$PRIVACY_RESULT" != "failure" ]]; then
            echo "The controlled mobile publish privacy check did not fail as expected." >&2
            exit 1
          fi
          grep -Fq -- "- Status: **BLOCKED**" "$GITHUB_STEP_SUMMARY"
          grep -Fq -- "privacy checks failed; store submission is blocked." "$GITHUB_STEP_SUMMARY"
          if [[ -e "$SUBMISSION_MARKER" ]]; then
            echo "The failed privacy check reached the simulated store submission boundary." >&2
            exit 1
          fi
          if grep -Fq -- "$fixture_sentinel" "$PRIVACY_CHECKER_SUMMARY" ||
            grep -Fq -- "$fixture_sentinel" "$GITHUB_STEP_SUMMARY"; then
            echo "The privacy summaries exposed controlled fixture contents." >&2
            exit 1
          fi

  native-evidence-tamper-regression:
    name: Native evidence tamper regression
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
      - android-release-runner-health
    permissions:
      actions: write
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify nested release evidence readers on hosted runner
        id: nested-release-evidence
        shell: bash
        run: |
          set -euo pipefail
          node --test \
            --test-name-pattern='every release JSON evidence reader rejects duplicate fields with fixed diagnostics|release evidence discovery catches an un-inventoried nested TypeScript helper reader|release evidence discovery follows helpers dynamically imported by shell checks|nested release evidence reader rejects duplicate fields with fixed redacted diagnostic' \
            scripts/tests/mobile-release-summary-contract.test.mjs

      - name: Summarize nested release evidence validation
        if: ${{ always() }}
        env:
          CHECK_RESULT: ${{ steps.nested-release-evidence.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "## Nested release evidence validation"
            echo
            echo "- Hosted nested-reader inventory guard: **${CHECK_RESULT^^}** (includes the expected un-inventoried-reader failure diagnostic)"
            echo "- Hosted duplicate-field diagnostic: **${CHECK_RESULT^^}** (fixed and redacted)"
            echo "- Evidence contents in release validation output: **NOT INCLUDED**"
          } >> "$GITHUB_STEP_SUMMARY"
          if [[ "$CHECK_RESULT" != "success" ]]; then
            echo "Nested release evidence validation failed; inspect the test result without publishing evidence contents." >&2
            exit 1
          fi

      - name: Create controlled native evidence fixture artifacts
        shell: bash
        run: |
          set -euo pipefail
          fixture_root="$RUNNER_TEMP/native-evidence-tamper-fixtures"
          run_timestamp=20260915T120000Z
          source scripts/tests/native-large-text-evidence-fixture.sh

          rm -rf "$fixture_root"
          write_native_large_text_evidence_fixture \
            "$fixture_root" ios controlled "$run_timestamp" \
            2026-09-15T12:00:00Z 2026-09-15T12:30:00Z
          write_native_large_text_evidence_fixture \
            "$fixture_root" android controlled "$run_timestamp" \
            2026-09-15T12:00:00Z 2026-09-15T12:30:00Z

      - name: Upload controlled iOS evidence fixture
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: native-evidence-tamper-fixture-ios
          path: ${{ runner.temp }}/native-evidence-tamper-fixtures/ios
          if-no-files-found: error

      - name: Upload controlled Android evidence fixture
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: native-evidence-tamper-fixture-android
          path: ${{ runner.temp }}/native-evidence-tamper-fixtures/android
          if-no-files-found: error

      - name: Download controlled iOS evidence fixture
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-evidence-tamper-fixture-ios
          path: ${{ runner.temp }}/native-evidence-tamper-downloads/ios

      - name: Download controlled Android evidence fixture
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-evidence-tamper-fixture-android
          path: ${{ runner.temp }}/native-evidence-tamper-downloads/android

      - name: Run strict tamper block check against downloaded fixtures
        id: tamper-check
        continue-on-error: true
        env:
          NATIVE_EVIDENCE_TAMPER_FIXTURES_ROOT: ${{ runner.temp }}/native-evidence-tamper-downloads
          NATIVE_EVIDENCE_TAMPER_DEADLINE: 20 minutes
          TAMPER_LOG_PATH: ${{ runner.temp }}/native-evidence-tamper-regression.log
        run: |
          set +e
          bash scripts/run-untrusted-checker.sh pnpm run test:native-large-text-evidence \
            > "$TAMPER_LOG_PATH" 2>&1
          tamper_status=$?
          set -e
          exit "$tamper_status"

      - name: Summarize native evidence tamper regression
        if: ${{ always() }}
        env:
          TAMPER_RESULT: ${{ steps.tamper-check.outcome }}
          TAMPER_LOG_PATH: ${{ runner.temp }}/native-evidence-tamper-regression.log
          TAMPER_DEADLINE: 20 minutes
        run: |
          set -euo pipefail
          {
            echo "## Hosted native evidence tamper regression"
            echo
            if [[ "$TAMPER_RESULT" == "success" ]]; then
              echo "- Status: **PASS**"
              echo "- Altered iOS fixture: **BLOCKED**; submission stub: **NOT INVOKED**"
              echo "- Altered Android fixture: **BLOCKED**; submission stub: **NOT INVOKED**"
            else
              echo "- Status: **BLOCKED**"
              echo "- Result: downloaded-fixture tamper validation failed; store submission is blocked."
            fi
            echo "- Validation deadline: **${TAMPER_DEADLINE}**"
          } >> "$GITHUB_STEP_SUMMARY"

          if [[ "$TAMPER_RESULT" == "success" ]]; then
            for platform in ios android; do
              if ! grep -Fq -- \
                "Hosted tampered ${platform} evidence recorded BLOCKED without invoking the submission stub. (validation deadline: ${TAMPER_DEADLINE})." \
                "$TAMPER_LOG_PATH"; then
                echo "Hosted tamper regression did not record the ${platform} BLOCKED result before the ${TAMPER_DEADLINE} deadline." >&2
                exit 1
              fi
            done
            if ! grep -Fq -- \
              "- Altered iOS fixture: **BLOCKED**; submission stub: **NOT INVOKED**" \
              "$GITHUB_STEP_SUMMARY" ||
              ! grep -Fq -- \
              "- Altered Android fixture: **BLOCKED**; submission stub: **NOT INVOKED**" \
              "$GITHUB_STEP_SUMMARY"; then
              echo "Hosted tamper regression summary did not preserve both platform BLOCKED results before the ${TAMPER_DEADLINE} deadline." >&2
              exit 1
            fi
          fi

          bash scripts/verify-native-tamper-output.sh \
            "$TAMPER_LOG_PATH" \
            "$GITHUB_STEP_SUMMARY"

          if [[ "$TAMPER_RESULT" != "success" ]]; then
            echo "Hosted tamper regression blocked release before the ${TAMPER_DEADLINE} deadline; inspect platform-specific diagnostics in the captured log." >&2
            exit 1
          fi

  native-ios:
    name: Native large-text smoke (iOS)
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    permissions:
      actions: write
      contents: read
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
      - android-release-runner-health
    outputs:
      native_evidence_artifact_url: ${{ steps.upload-ios-native-smoke.outputs.artifact-url }}
      native_evidence_digest_manifest: ${{ steps.ios-native-evidence-digest.outputs.native_evidence_digest_manifest }}
    runs-on:
      - self-hosted
      - macos
      - ios
      - smallest-simulator
    timeout-minutes: 90
    environment:
      name: mobile-release
    env:
      NATIVE_SMOKE_RESULTS_DIR: ${{ github.workspace }}/test-results/native-large-text/ios/${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Clear prior iOS evidence from this runner
        if: ${{ always() }}
        run: rm -rf test-results/native-large-text/ios

      - name: Check iOS release prerequisites
        env:
          NATIVE_SMOKE_IOS_APP_ID: ${{ secrets.NATIVE_SMOKE_IOS_APP_ID }}
          NATIVE_SMOKE_EMAIL: ${{ secrets.NATIVE_SMOKE_EMAIL }}
          NATIVE_SMOKE_PASSWORD: ${{ secrets.NATIVE_SMOKE_PASSWORD }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          NATIVE_SMOKE_IOS_SENTRY_RELEASE: ${{ secrets.NATIVE_SMOKE_IOS_SENTRY_RELEASE }}
          NATIVE_SMOKE_IOS_SENTRY_DIST: ${{ secrets.NATIVE_SMOKE_IOS_SENTRY_DIST }}
          NATIVE_SMOKE_IOS_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
        run: bash scripts/run-untrusted-checker.sh bash scripts/check-ios-release-prerequisites.sh

      - name: Set up pnpm
        id: setup-pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        id: setup-node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}
          cache: pnpm

      - name: Install workspace dependencies
        id: install-dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify app branding
        id: app-branding
        run: bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app run validate:branding

      - name: Verify iOS release configuration
        id: ios-release-configuration
        env:
          NATIVE_SMOKE_IOS_APP_ID: ${{ secrets.NATIVE_SMOKE_IOS_APP_ID }}
          NATIVE_SMOKE_IOS_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          NATIVE_SMOKE_EMAIL: ${{ secrets.NATIVE_SMOKE_EMAIL }}
          NATIVE_SMOKE_PASSWORD: ${{ secrets.NATIVE_SMOKE_PASSWORD }}
        run: |
          set -euo pipefail
          for value in \
            NATIVE_SMOKE_IOS_APP_ID \
            NATIVE_SMOKE_IOS_BUILD_ID \
            NATIVE_SMOKE_EMAIL \
            NATIVE_SMOKE_PASSWORD; do
            if [[ -z "${!value:-}" ]]; then
              echo "Required iOS release configuration is incomplete." >&2
              exit 2
            fi
          done

      - name: Verify iOS simulator tooling
        id: ios-xcrun
        run: |
          set -euo pipefail
          if ! command -v xcrun >/dev/null 2>&1; then
            echo "xcrun is required on the prepared iOS runner." >&2
            exit 2
          fi

      - name: Verify prepared iPhone SE simulator
        id: ios-simulator
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          booted_devices="$(xcrun simctl list devices booted)"
          device_udid="$(
            sed -n 's/^[[:space:]]*iPhone SE (3rd generation) (\([0-9A-F-]\{8,\}\)) (Booted)[[:space:]]*$/\1/p' <<<"$booted_devices" |
              head -n 1
          )"
          if [[ -z "$device_udid" ]]; then
            echo "A booted iPhone SE (3rd generation) is required on the iOS runner." >&2
            xcrun simctl list devices booted 2>&1 | sanitize_workflow_stream >&2 || true
            exit 2
          fi
          printf 'NATIVE_SMOKE_IOS_DEVICE_UDID=%s\n' "$device_udid" >> "$GITHUB_ENV"
          echo "Prepared iPhone SE (3rd generation): $(sanitize_workflow_text "$device_udid")"

      - name: Inspect installed iOS native branding
        id: ios-native-branding
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_IOS_APP_ID }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          NATIVE_SMOKE_IOS_DEVICE_UDID: ${{ env.NATIVE_SMOKE_IOS_DEVICE_UDID }}
        run: |
          set -euo pipefail
          mkdir -p "$NATIVE_SMOKE_RESULTS_DIR"
          device_udid="${NATIVE_SMOKE_IOS_DEVICE_UDID:-}"
          if [[ -z "$device_udid" ]]; then
            echo "The prepared iPhone SE simulator UDID was not carried forward from verification." >&2
            exit 2
          fi
          app_container="$(xcrun simctl get_app_container "$device_udid" "$NATIVE_SMOKE_APP_ID" app)"
          if [[ ! -f "$app_container/Info.plist" ]]; then
            echo "The release candidate is not installed on the prepared iOS simulator." >&2
            exit 2
          fi
          if ! LC_ALL=C grep -aR -Fq "SENTRY_RELEASE_PREFLIGHT_PASSED_V1" "$app_container"; then
            echo "The installed iOS candidate does not contain crash-reporting preflight evidence. Rebuild it with SENTRY_DSN or EXPO_PUBLIC_SENTRY_DSN configured." >&2
            exit 2
          fi
          echo "Verified crash-reporting preflight evidence in the installed iOS candidate."
          metadata_path="$NATIVE_SMOKE_RESULTS_DIR/native-info.json"
          plutil -convert json -o "$metadata_path" "$app_container/Info.plist"
          bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app run validate:branding:native -- \
            --platform ios \
            --metadata "$metadata_path" \
            --build-id "$NATIVE_SMOKE_BUILD_ID" \
            --results-dir "$NATIVE_SMOKE_RESULTS_DIR"

      - name: Verify Maestro
        id: ios-maestro
        run: maestro --version

      - name: Run iOS native large-text release gate
        id: ios-native-evidence
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_IOS_APP_ID }}
          NATIVE_SMOKE_EMAIL: ${{ secrets.NATIVE_SMOKE_EMAIL }}
          NATIVE_SMOKE_PASSWORD: ${{ secrets.NATIVE_SMOKE_PASSWORD }}
          NATIVE_SMOKE_DISPLAY_NAME: ${{ secrets.NATIVE_SMOKE_DISPLAY_NAME }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          NATIVE_SMOKE_IOS_DEVICE_UDID: ${{ env.NATIVE_SMOKE_IOS_DEVICE_UDID }}
          NATIVE_SMOKE_SUMMARY_DEFER: "1"
        run: bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app test:native-large-text ios

      - name: Send controlled iOS JavaScript error
        id: ios-sentry-trigger
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_IOS_APP_ID }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          NATIVE_SMOKE_IOS_DEVICE_UDID: ${{ env.NATIVE_SMOKE_IOS_DEVICE_UDID }}
          NATIVE_SENTRY_MARKER: native-${{ github.run_id }}-${{ github.run_attempt }}-ios
        run: bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app test:native-sentry ios

      - name: Verify readable iOS source maps in Sentry
        id: ios-sentry-verification
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_EXPECTED_PLATFORM: ios
          SENTRY_EXPECTED_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          SENTRY_EXPECTED_RELEASE: ${{ secrets.NATIVE_SMOKE_IOS_SENTRY_RELEASE }}
          SENTRY_EXPECTED_DIST: ${{ secrets.NATIVE_SMOKE_IOS_SENTRY_DIST }}
          SENTRY_PROBE_MARKER: native-${{ github.run_id }}-${{ github.run_attempt }}-ios
          SENTRY_EVIDENCE_PATH: ${{ env.NATIVE_SMOKE_RESULTS_DIR }}/sentry-source-map-evidence.json
        run: bash scripts/run-untrusted-checker.sh node scripts/verify-sentry-native-event.mjs

      - name: Check iOS native evidence text sizes before upload
        id: ios-native-evidence-size
        if: ${{ always() }}
        run: bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh \
          --check-collection-size "$NATIVE_SMOKE_RESULTS_DIR"

      - name: Summarize iOS readiness
        if: always()
        env:
          IOS_RESULTS_DIR: ${{ env.NATIVE_SMOKE_RESULTS_DIR }}
          WORKSPACE_RESULT: ${{ steps.install-dependencies.outcome }}
          APP_BRANDING_RESULT: ${{ steps.app-branding.outcome }}
          IOS_RELEASE_CONFIGURATION_RESULT: ${{ steps.ios-release-configuration.outcome }}
          IOS_XCRUN_RESULT: ${{ steps.ios-xcrun.outcome }}
          IOS_SIMULATOR_RESULT: ${{ steps.ios-simulator.outcome }}
          IOS_NATIVE_BRANDING_RESULT: ${{ steps.ios-native-branding.outcome }}
          IOS_MAESTRO_RESULT: ${{ steps.ios-maestro.outcome }}
          IOS_NATIVE_EVIDENCE_RESULT: ${{ steps.ios-native-evidence.outcome }}
          IOS_SENTRY_TRIGGER_RESULT: ${{ steps.ios-sentry-trigger.outcome }}
          IOS_SENTRY_VERIFICATION_RESULT: ${{ steps.ios-sentry-verification.outcome }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          status="READY"
          blockers=()
          checks=()

          add_check() {
            local label="$1"
            local result="$2"
            if [[ "$result" == "success" ]]; then
              checks+=("- ${label}: **READY**")
            else
              status="BLOCKED"
              checks+=("- ${label}: **BLOCKED**")
              blockers+=("${label}")
            fi
          }

          add_check "Workspace dependencies" "$WORKSPACE_RESULT"
          add_check "App branding source" "$APP_BRANDING_RESULT"
          add_check "Release configuration" "$IOS_RELEASE_CONFIGURATION_RESULT"
          add_check "Xcode simulator tooling" "$IOS_XCRUN_RESULT"
          add_check "Booted iPhone SE (3rd generation)" "$IOS_SIMULATOR_RESULT"
          add_check "Installed native candidate" "$IOS_NATIVE_BRANDING_RESULT"
          add_check "Maestro" "$IOS_MAESTRO_RESULT"
          add_check "Native large-text evidence" "$IOS_NATIVE_EVIDENCE_RESULT"
          add_check "Controlled Sentry error" "$IOS_SENTRY_TRIGGER_RESULT"
          add_check "Readable Sentry source maps" "$IOS_SENTRY_VERIFICATION_RESULT"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
            echo
            echo "## iOS native large-text readiness"
            echo
            echo "- Status: **${status}**"
            echo
            echo "### Prerequisites"
            printf '%s\n' "${checks[@]}"
            if [[ -f "$IOS_RESULTS_DIR/ios-readiness.md" ]]; then
              diagnostics="$(
                awk '
                  /^### Blocking prerequisites$/ { collecting=1; next }
                  collecting && /^### / { exit }
                  collecting && substr($0, 1, 3) == "```" { next }
                  collecting { print }
                ' "$IOS_RESULTS_DIR/ios-readiness.md"
              )"
              if [[ -n "$diagnostics" ]]; then
                echo
                echo "### Readiness diagnostics"
                printf '%s\n' "$diagnostics" |
                  sanitize_workflow_stream |
                  render_markdown_code_block
              fi
            fi
            if ((${#blockers[@]})); then
              echo
              echo "### Blocking prerequisites"
              printf -- '- %s\n' "${blockers[@]}"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Create trusted iOS native evidence digest manifest
        id: ios-native-evidence-digest
        if: ${{ always() && steps.ios-native-evidence-size.outcome == 'success' }}
        run: |
          set -euo pipefail
          manifest="$(
            cd "$NATIVE_SMOKE_RESULTS_DIR"
            find . -type f ! -name review-record.txt -print |
              LC_ALL=C sort |
              while IFS= read -r relative_path; do
                if command -v sha256sum >/dev/null 2>&1; then
                  digest="$(sha256sum "$relative_path" | awk '{ print $1 }')"
                else
                  digest="$(shasum -a 256 "$relative_path" | awk '{ print $1 }')"
                fi
                printf '%s  %s\n' "$digest" "${relative_path#./}"
              done
          )"
          {
            echo 'native_evidence_digest_manifest<<NATIVE_EVIDENCE_DIGEST_MANIFEST'
            printf '%s\n' "$manifest"
            echo NATIVE_EVIDENCE_DIGEST_MANIFEST
          } >> "$GITHUB_OUTPUT"

      - name: Upload iOS native smoke artifacts
        if: ${{ always() && steps.ios-native-evidence-size.outcome == 'success' }}
        id: upload-ios-native-smoke
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: native-large-text-ios
          path: test-results/native-large-text/ios/**
          if-no-files-found: warn
          # Native report links are used for historical release review. Keep
          # the fixed artifact through GitHub's maximum supported retention
          # window; the release gate still downloads it before publishing a
          # link so expired artifacts cannot be presented as available.
          retention-days: 90
          overwrite: true

      - name: Summarize iOS native branding
        if: always()
        env:
          NATIVE_BRANDING_ARTIFACT_URL: ${{ steps.upload-ios-native-smoke.outputs.artifact-url }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
            echo
          } >> "$GITHUB_STEP_SUMMARY"
          scripts/summarize-native-branding.sh ios

  android-prerequisite-preflight:
    name: Android release runner preflight
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
      - android-release-runner-health
    runs-on:
      - self-hosted
      - linux
      - android
      - smallest-simulator
    timeout-minutes: 20
    environment:
      name: mobile-release
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Check Android release prerequisites
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_ANDROID_APP_ID }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
          NATIVE_SMOKE_EMAIL: ${{ secrets.NATIVE_SMOKE_EMAIL }}
          NATIVE_SMOKE_PASSWORD: ${{ secrets.NATIVE_SMOKE_PASSWORD }}
          NATIVE_SMOKE_DISPLAY_NAME: ${{ secrets.NATIVE_SMOKE_DISPLAY_NAME }}
        run: bash scripts/run-untrusted-checker.sh bash scripts/check-android-release-prerequisites.sh

  native-android:
    name: Native large-text smoke (Android)
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    permissions:
      actions: write
      contents: read
    outputs:
      native_evidence_artifact_url: ${{ steps.upload-android-native-smoke.outputs.artifact-url }}
      native_evidence_digest_manifest: ${{ steps.android-native-evidence-digest.outputs.native_evidence_digest_manifest }}
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
      - android-release-runner-health
      - android-prerequisite-preflight
    runs-on:
      - self-hosted
      - linux
      - android
      - smallest-simulator
    timeout-minutes: 90
    environment:
      name: mobile-release
    env:
      NATIVE_SMOKE_RESULTS_DIR: ${{ github.workspace }}/test-results/native-large-text/android/${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Clear prior Android evidence from this runner
        run: rm -rf test-results/native-large-text/android

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify app branding
        run: bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app run validate:branding

      - name: Verify prepared smallest Android emulator
        run: |
          set -euo pipefail
          if ! command -v adb >/dev/null 2>&1; then
            echo "adb is required on the prepared Android runner." >&2
            exit 2
          fi
          timeout 60s adb wait-for-device
          adb get-state >/dev/null
          size="$(adb shell wm size | tr -d '\r' | tail -n 1 | sed 's/^.*: //')"
          density="$(adb shell wm density | tr -d '\r' | tail -n 1 | sed 's/^.*: //')"
          IFS=x read -r width_px height_px <<<"$size"
          if [[ ! "$width_px" =~ ^[0-9]+$ || ! "$height_px" =~ ^[0-9]+$ || ! "$density" =~ ^[0-9]+$ ]]; then
            echo "Could not determine Android emulator size and density." >&2
            exit 2
          fi
          width_dp=$((width_px * 160 / density))
          height_dp=$((height_px * 160 / density))
          if ((width_dp > 320 || height_dp > 568)); then
            echo "The Android runner must use an emulator at or below 320x568 dp; found ${width_dp}x${height_dp} dp." >&2
            exit 2
          fi
          rotation="$(adb shell settings get system user_rotation | tr -d '\r' | tr -d '[:space:]')"
          if [[ "$rotation" != "0" && "$rotation" != "2" ]]; then
            echo "The Android runner must be in portrait orientation; found user_rotation=${rotation:-unknown}." >&2
            exit 2
          fi

      - name: Inspect installed Android native branding
        id: android-native-branding
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_ANDROID_APP_ID }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
        run: |
          set -euo pipefail
          # Keep installed-APK inspection on the same reviewed build-tools
          # version enforced by the Android runner preflight.
          source scripts/android-runner-pins.sh
          mkdir -p "$NATIVE_SMOKE_RESULTS_DIR"
          sdk_root="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-}}"
          aapt2_path=""
          if [[ -n "$sdk_root" ]]; then
            aapt2_path="$sdk_root/build-tools/$ANDROID_BUILD_TOOLS_VERSION/aapt2"
          fi
          if [[ -z "$aapt2_path" || ! -x "$aapt2_path" ]]; then
            echo "aapt2 is required to inspect the compiled Android manifest." >&2
            exit 2
          fi
          apk_source="$(adb shell pm path "$NATIVE_SMOKE_APP_ID" | tr -d '\r' | sed -n 's/^package://p' | head -n 1)"
          if [[ -z "$apk_source" ]]; then
            echo "Could not locate the release candidate APK on the prepared Android device." >&2
            exit 2
          fi
          apk_path="$RUNNER_TEMP/realtime-algo-chat-app.apk"
          adb pull "$apk_source" "$apk_path"
          apk_extract_dir="$RUNNER_TEMP/realtime-algo-chat-app-extracted"
          rm -rf "$apk_extract_dir"
          unzip -q "$apk_path" -d "$apk_extract_dir"
          if ! LC_ALL=C grep -aR -Fq "SENTRY_RELEASE_PREFLIGHT_PASSED_V1" "$apk_extract_dir"; then
            echo "The installed Android candidate does not contain crash-reporting preflight evidence. Rebuild it with SENTRY_DSN or EXPO_PUBLIC_SENTRY_DSN configured." >&2
            exit 2
          fi
          echo "Verified crash-reporting preflight evidence in the installed Android candidate."
          badging_path="$NATIVE_SMOKE_RESULTS_DIR/android-badging.txt"
          "$aapt2_path" dump badging "$apk_path" > "$badging_path"
          metadata_path="$NATIVE_SMOKE_RESULTS_DIR/native-info.json"
          node --input-type=module - "$badging_path" "$metadata_path" <<'NODE'
          import { readFileSync, writeFileSync } from "node:fs";

          const [, , badgingPath, metadataPath] = process.argv;
          const badging = readFileSync(badgingPath, "utf8");
          const label = badging.match(/^application-label:'([^']*)'$/m)?.[1];
          const permissions = [...badging.matchAll(
            /^uses-permission: name='([^']+)'/gm,
          )].map((match) => match[1]);
          // A label aapt2 did not report is left out so the branding validator
          // fails on the missing field and its summary names it, instead of
          // this step failing before any report exists.
          const metadata =
            label === undefined ? { permissions } : { applicationLabel: label, permissions };
          writeFileSync(metadataPath, `${JSON.stringify(metadata, null, 2)}\n`);
          NODE
          bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app run validate:branding:native -- \
            --platform android \
            --metadata "$metadata_path" \
            --build-id "$NATIVE_SMOKE_BUILD_ID" \
            --results-dir "$NATIVE_SMOKE_RESULTS_DIR"

      - name: Verify Maestro
        run: maestro --version

      - name: Run Android native large-text release gate
        id: android-native-evidence
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_ANDROID_APP_ID }}
          NATIVE_SMOKE_EMAIL: ${{ secrets.NATIVE_SMOKE_EMAIL }}
          NATIVE_SMOKE_PASSWORD: ${{ secrets.NATIVE_SMOKE_PASSWORD }}
          NATIVE_SMOKE_DISPLAY_NAME: ${{ secrets.NATIVE_SMOKE_DISPLAY_NAME }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
        run: bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app test:native-large-text android

      - name: Send controlled Android JavaScript error
        id: android-sentry-trigger
        env:
          NATIVE_SMOKE_APP_ID: ${{ secrets.NATIVE_SMOKE_ANDROID_APP_ID }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
          NATIVE_SENTRY_MARKER: native-${{ github.run_id }}-${{ github.run_attempt }}-android
        run: bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/chat-app test:native-sentry android

      - name: Verify readable Android source maps in Sentry
        id: android-sentry-verification
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_EXPECTED_PLATFORM: android
          SENTRY_EXPECTED_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
          SENTRY_EXPECTED_RELEASE: ${{ secrets.NATIVE_SMOKE_ANDROID_SENTRY_RELEASE }}
          SENTRY_EXPECTED_DIST: ${{ secrets.NATIVE_SMOKE_ANDROID_SENTRY_DIST }}
          SENTRY_PROBE_MARKER: native-${{ github.run_id }}-${{ github.run_attempt }}-android
          SENTRY_EVIDENCE_PATH: ${{ env.NATIVE_SMOKE_RESULTS_DIR }}/sentry-source-map-evidence.json
        run: bash scripts/run-untrusted-checker.sh node scripts/verify-sentry-native-event.mjs

      - name: Check Android native evidence text sizes before upload
        id: android-native-evidence-size
        if: ${{ always() }}
        run: bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh \
          --check-collection-size "$NATIVE_SMOKE_RESULTS_DIR"

      - name: Create trusted Android native evidence digest manifest
        id: android-native-evidence-digest
        if: ${{ always() && steps.android-native-evidence-size.outcome == 'success' }}
        run: |
          set -euo pipefail
          manifest="$(
            cd "$NATIVE_SMOKE_RESULTS_DIR"
            find . -type f ! -name review-record.txt -print |
              LC_ALL=C sort |
              while IFS= read -r relative_path; do
                if command -v sha256sum >/dev/null 2>&1; then
                  digest="$(sha256sum "$relative_path" | awk '{ print $1 }')"
                else
                  digest="$(shasum -a 256 "$relative_path" | awk '{ print $1 }')"
                fi
                printf '%s  %s\n' "$digest" "${relative_path#./}"
              done
          )"
          {
            echo 'native_evidence_digest_manifest<<NATIVE_EVIDENCE_DIGEST_MANIFEST'
            printf '%s\n' "$manifest"
            echo NATIVE_EVIDENCE_DIGEST_MANIFEST
          } >> "$GITHUB_OUTPUT"

      - name: Upload Android native smoke artifacts
        if: ${{ always() && steps.android-native-evidence-size.outcome == 'success' }}
        id: upload-android-native-smoke
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: native-large-text-android
          path: test-results/native-large-text/android/**
          if-no-files-found: warn
          # Keep the fixed native report link through the full historical
          # review window. The release gate verifies availability before
          # carrying the link into its summary.
          retention-days: 90
          overwrite: true

      - name: Summarize Android native branding
        if: always()
        env:
          NATIVE_BRANDING_ARTIFACT_URL: ${{ steps.upload-android-native-smoke.outputs.artifact-url }}
          NATIVE_SMOKE_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
            echo
          } >> "$GITHUB_STEP_SUMMARY"
          scripts/summarize-native-branding.sh android

  idle-profile-browser-evidence-contract:
    name: Idle-profile browser evidence contract
    permissions:
      actions: write
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium for controlled browser failure
        run: pnpm --filter @workspace/api-server exec playwright install --with-deps chromium

      - name: Run controlled idle-profile browser failure
        id: idle-profile-evidence-check
        continue-on-error: true
        env:
          E2E_RECOVERY_DIAGNOSTIC_CONTRACT: "1"
          IDLE_PROFILE_EVIDENCE_CONTRACT: "1"
          PLAYWRIGHT_LAST_RUN_OUTPUT_FILE: ${{ runner.temp }}/idle-profile-evidence-last-run.json
        run: |
          set -euo pipefail
          rm -rf artifacts/api-server/test-results
          pnpm --filter @workspace/api-server exec playwright test \
            e2e/idle-profile-registration.spec.ts \
            --config e2e/playwright.config.ts \
            --grep "controlled browser failure"

      - name: Upload controlled idle-profile browser evidence
        if: ${{ always() }}
        continue-on-error: true
        id: upload-idle-profile-evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: idle-profile-registration-browser-evidence-contract
          path: artifacts/api-server/test-results/**
          if-no-files-found: error
          retention-days: 14

      - name: Download controlled idle-profile browser evidence
        if: ${{ always() }}
        continue-on-error: true
        id: download-idle-profile-evidence
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: idle-profile-registration-browser-evidence-contract
          path: ${{ runner.temp }}/idle-profile-browser-evidence-contract

      - name: Verify controlled idle-profile browser evidence
        if: ${{ always() }}
        env:
          CONTROLLED_CHECK_RESULT: ${{ steps.idle-profile-evidence-check.outcome }}
          DOWNLOAD_RESULT: ${{ steps.download-idle-profile-evidence.outcome }}
        run: |
          set -euo pipefail
          if [[ "$CONTROLLED_CHECK_RESULT" != "failure" ]]; then
            echo "The controlled idle-profile browser test did not fail as expected." >&2
            exit 1
          fi
          if [[ "$DOWNLOAD_RESULT" != "success" ]]; then
            echo "The controlled idle-profile browser evidence artifact was not downloadable." >&2
            exit 1
          fi
          bash scripts/check-idle-profile-browser-evidence.sh \
            "$RUNNER_TEMP/idle-profile-browser-evidence-contract"

      - name: Summarize idle-profile browser evidence contract
        if: ${{ always() }}
        env:
          CONTROLLED_CHECK_RESULT: ${{ steps.idle-profile-evidence-check.outcome }}
          UPLOAD_RESULT: ${{ steps.upload-idle-profile-evidence.outcome }}
          DOWNLOAD_RESULT: ${{ steps.download-idle-profile-evidence.outcome }}
        run: |
          set -euo pipefail
          {
            echo "## Idle-profile browser evidence contract"
            echo
            printf -- '- Controlled browser failure: **%s**\n' "$CONTROLLED_CHECK_RESULT"
            printf -- '- Artifact upload: **%s**\n' "$UPLOAD_RESULT"
            printf -- '- Artifact download: **%s**\n' "$DOWNLOAD_RESULT"
            echo "- Expected archive members: \`test-failed-1.png\`, \`trace.zip\`, and \`error-context.md\`"
            echo "- Review the downloadable \`idle-profile-registration-browser-evidence-contract\` artifact when diagnosing a contract failure."
          } >> "$GITHUB_STEP_SUMMARY"

  idle-profile-registration:
    name: Idle profile registration
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true }}
    permissions:
      actions: write
      contents: read
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
      - android-release-runner-health
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment:
      name: mobile-release
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Determine idle-profile secret configuration
        id: idle-profile-config
        env:
          E2E_CHAT_URL: ${{ secrets.E2E_CHAT_URL }}
          E2E_API_URL: ${{ secrets.E2E_API_URL }}
          CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          set -euo pipefail
          if [[ -n "${E2E_CHAT_URL:-}" && -n "${E2E_API_URL:-}" ]]; then
            echo "browser_targets_configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "browser_targets_configured=false" >> "$GITHUB_OUTPUT"
          fi
          if [[ -n "${E2E_CHAT_URL:-}" && -n "${E2E_API_URL:-}" && -n "${CLERK_PUBLISHABLE_KEY:-}" && -n "${CLERK_SECRET_KEY:-}" && -n "${DATABASE_URL:-}" ]]; then
            echo "release_check_configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "release_check_configured=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Verify idle-profile Chat App target
        id: idle-profile-chat-preflight
        if: ${{ steps.idle-profile-config.outputs.browser_targets_configured == 'true' }}
        env:
          E2E_CHAT_URL: ${{ secrets.E2E_CHAT_URL }}
        run: |
          set -euo pipefail
          if ! curl \
            --fail \
            --silent \
            --connect-timeout 5 \
            --max-time 10 \
            --output /dev/null \
            "$E2E_CHAT_URL"; then
            echo "Chat App target is unavailable or unhealthy." >&2
            exit 2
          fi
          echo "Chat App target is ready."

      - name: Verify idle-profile API target
        id: idle-profile-api-preflight
        if: ${{ steps.idle-profile-config.outputs.browser_targets_configured == 'true' }}
        env:
          E2E_API_URL: ${{ secrets.E2E_API_URL }}
        run: |
          set -euo pipefail
          # Release checks need database-backed readiness. Deployment health
          # and the Sentry uptime monitor intentionally use /api/livez.
          if ! curl \
            --fail \
            --silent \
            --connect-timeout 5 \
            --max-time 10 \
            --output /dev/null \
            "${E2E_API_URL%/}/api/healthz"; then
            echo "API target is unavailable or unhealthy." >&2
            exit 2
          fi
          echo "API target is ready."

      - name: Record idle-profile browser target preflight
        id: idle-profile-preflight
        if: ${{ always() && steps.idle-profile-config.outputs.browser_targets_configured == 'true' }}
        env:
          IDLE_PROFILE_CHAT_PREFLIGHT_RESULT: ${{ steps.idle-profile-chat-preflight.outcome }}
          IDLE_PROFILE_API_PREFLIGHT_RESULT: ${{ steps.idle-profile-api-preflight.outcome }}
        run: |
          set -euo pipefail
          if [[ "$IDLE_PROFILE_CHAT_PREFLIGHT_RESULT" != "success" ]]; then
            echo "failure_reason=chat-app-target-unavailable" >> "$GITHUB_OUTPUT"
          elif [[ "$IDLE_PROFILE_API_PREFLIGHT_RESULT" != "success" ]]; then
            echo "failure_reason=api-target-unavailable" >> "$GITHUB_OUTPUT"
          else
            echo "failure_reason=" >> "$GITHUB_OUTPUT"
          fi

      - name: Run idle-profile registration release check
        id: idle-profile-check
        if: ${{ steps.idle-profile-config.outputs.release_check_configured == 'true' }}
        env:
          E2E_CHAT_URL: ${{ secrets.E2E_CHAT_URL }}
          E2E_API_URL: ${{ secrets.E2E_API_URL }}
          CLERK_PUBLISHABLE_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          set -euo pipefail
          for value in \
            E2E_CHAT_URL \
            E2E_API_URL \
            CLERK_PUBLISHABLE_KEY \
            CLERK_SECRET_KEY \
            DATABASE_URL; do
            if [[ -z "${!value:-}" ]]; then
              echo "Required idle-profile release configuration is incomplete." >&2
              exit 2
            fi
          done
          bash scripts/run-untrusted-checker.sh pnpm --filter @workspace/api-server run test:e2e:idle-profile-registration

      - name: Upload idle-profile browser failure evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: idle-profile-registration-browser-evidence
          path: artifacts/api-server/test-results/**
          if-no-files-found: warn
          retention-days: 14
          overwrite: true

      - name: Summarize idle-profile registration check
        if: ${{ always() }}
        env:
          IDLE_PROFILE_PREFLIGHT_FAILURE_REASON: ${{ steps.idle-profile-preflight.outputs.failure_reason }}
          IDLE_PROFILE_RESULT: ${{ steps.idle-profile-check.outcome }}
          REVIEWED_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"
          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
            echo
            echo "## Idle profile registration"
            echo
            if [[ "$IDLE_PROFILE_RESULT" == "success" ]]; then
              echo "- Status: **PASS**"
              echo "- Result: the idle client did not repeat \`PUT /api/profile\` after token refresh."
            elif [[ -n "$IDLE_PROFILE_PREFLIGHT_FAILURE_REASON" ]]; then
              echo "- Status: **FAIL**"
              case "$IDLE_PROFILE_PREFLIGHT_FAILURE_REASON" in
                chat-app-target-unavailable)
                  echo "- Result: release is blocked because the Chat App target is unavailable or unhealthy."
                  ;;
                api-target-unavailable)
                  echo "- Result: release is blocked because the API target is unavailable or unhealthy."
                  ;;
                *)
                  echo "- Result: release is blocked because idle-profile browser target preflight failed."
                  ;;
              esac
            elif [[ "$IDLE_PROFILE_RESULT" == "skipped" ]]; then
              echo "- Status: **SKIP**"
              echo "- Result: check was skipped because idle-profile secrets are not configured."
            else
              echo "- Status: **FAIL**"
              echo "- Result: release is blocked because the idle-profile registration check did not pass."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  mobile-release-gate:
    name: Mobile release gate
    if: ${{ github.event_name != 'pull_request' && inputs.android_runner_health_only != true && needs.mobile-release-node-range.result == 'success' && needs.mobile-release-credentials.result == 'success' && needs.native-ios.result == 'success' && needs.native-android.result == 'success' && needs.idle-profile-registration.result == 'success' && needs.native-evidence-summary-regression.result == 'success' && needs.native-evidence-tamper-regression.result == 'success' }}
    outputs:
      ios_native_evidence_artifact_url: ${{ needs.native-ios.outputs.native_evidence_artifact_url }}
      android_native_evidence_artifact_url: ${{ needs.native-android.outputs.native_evidence_artifact_url }}
      ios_native_evidence_digest_manifest: ${{ needs.native-ios.outputs.native_evidence_digest_manifest }}
      android_native_evidence_digest_manifest: ${{ needs.native-android.outputs.native_evidence_digest_manifest }}
    needs:
      - mobile-release-node-range
      - mobile-release-credentials
      - android-release-runner-health
      - native-ios
      - native-android
      - idle-profile-registration
      - native-evidence-summary-regression
      - native-evidence-tamper-regression
    runs-on: ubuntu-latest
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Download iOS native smoke artifacts
        if: ${{ always() }}
        continue-on-error: true
        id: download-ios-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-ios
          path: test-results/native-large-text/ios

      - name: Clear partial iOS native smoke artifacts before retry
        if: ${{ always() && steps.download-ios-native-smoke.outcome != 'success' }}
        continue-on-error: true
        id: cleanup-ios-native-smoke
        run: rm -rf -- test-results/native-large-text/ios && mkdir -p -- test-results/native-large-text/ios

      - name: Retry iOS native smoke artifact download
        if: ${{ always() && steps.download-ios-native-smoke.outcome != 'success' && steps.cleanup-ios-native-smoke.outcome == 'success' }}
        continue-on-error: true
        id: retry-ios-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-ios
          path: test-results/native-large-text/ios

      - name: Download Android native smoke artifacts
        if: ${{ always() }}
        continue-on-error: true
        id: download-android-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-android
          path: test-results/native-large-text/android

      - name: Clear partial Android native smoke artifacts before retry
        if: ${{ always() && steps.download-android-native-smoke.outcome != 'success' }}
        continue-on-error: true
        id: cleanup-android-native-smoke
        run: rm -rf -- test-results/native-large-text/android && mkdir -p -- test-results/native-large-text/android

      - name: Retry Android native smoke artifact download
        if: ${{ always() && steps.download-android-native-smoke.outcome != 'success' && steps.cleanup-android-native-smoke.outcome == 'success' }}
        continue-on-error: true
        id: retry-android-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-android
          path: test-results/native-large-text/android

      - name: Validate native evidence completeness
        id: evidence-completeness
        if: ${{ always() }}
        continue-on-error: true
        env:
          REVIEWED_REF: ${{ github.ref }}
          NATIVE_IOS_EVIDENCE_ARTIFACT_URL: ${{ needs.native-ios.outputs.native_evidence_artifact_url }}
          NATIVE_ANDROID_EVIDENCE_ARTIFACT_URL: ${{ needs.native-android.outputs.native_evidence_artifact_url }}
          NATIVE_IOS_EVIDENCE_DIGEST_MANIFEST: ${{ needs.native-ios.outputs.native_evidence_digest_manifest }}
          NATIVE_ANDROID_EVIDENCE_DIGEST_MANIFEST: ${{ needs.native-android.outputs.native_evidence_digest_manifest }}
          NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.download-ios-native-smoke.outcome == 'success' && 'success' || steps.retry-ios-native-smoke.outcome }}
          NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.download-android-native-smoke.outcome == 'success' && 'success' || steps.retry-android-native-smoke.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh

      - name: Block release unless both native checks pass
        if: ${{ always() }}
        env:
          IOS_RESULT: ${{ needs.native-ios.result }}
          ANDROID_RESULT: ${{ needs.native-android.result }}
          IDLE_PROFILE_RESULT: ${{ needs.idle-profile-registration.result }}
          SUMMARY_REGRESSION_RESULT: ${{ needs.native-evidence-summary-regression.result }}
          TAMPER_REGRESSION_RESULT: ${{ needs.native-evidence-tamper-regression.result }}
          EVIDENCE_RESULT: ${{ steps.evidence-completeness.outcome }}
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          supported_node_range="$(
            node --input-type=module -e '
            import { readFileSync } from "node:fs";

            try {
              const packageJson = JSON.parse(readFileSync("package.json", "utf8"));
              const configuredRange = packageJson?.engines?.node;
              if (typeof configuredRange !== "string" || configuredRange.trim() === "") {
                process.stdout.write(
                  "missing or non-string value (set package.json engines.node to a valid semver range)",
                );
              } else {
                process.stdout.write(JSON.stringify(configuredRange.trim()));
              }
            } catch {
              process.stdout.write(
                "unavailable because package.json is not valid JSON (fix package.json before rerunning)",
              );
            }
            '
          )"
          safe_supported_node_range="$(sanitize_workflow_text "$supported_node_range")"
          {
            echo "## Mobile release gate"
            echo
            echo "- Native evidence completeness: **${EVIDENCE_RESULT^^}**"
            echo "- iOS native smoke: **${IOS_RESULT^^}**"
            echo "- Android native smoke: **${ANDROID_RESULT^^}**"
            echo "- Idle profile registration: **${IDLE_PROFILE_RESULT^^}**"
            echo "- Native evidence summary regression: **${SUMMARY_REGRESSION_RESULT^^}**"
            echo "- Native evidence tamper regression: **${TAMPER_REGRESSION_RESULT^^}**"
            echo "- Supported Node.js range: ${safe_supported_node_range}"
          } >> "$GITHUB_STEP_SUMMARY"
          if [[ "$EVIDENCE_RESULT" != "success" || "$IOS_RESULT" != "success" || "$ANDROID_RESULT" != "success" || "$IDLE_PROFILE_RESULT" != "success" || "$SUMMARY_REGRESSION_RESULT" != "success" || "$TAMPER_REGRESSION_RESULT" != "success" ]]; then
            echo "Mobile release blocked: evidence=$EVIDENCE_RESULT iOS=$IOS_RESULT Android=$ANDROID_RESULT idle-profile=$IDLE_PROFILE_RESULT summary-regression=$SUMMARY_REGRESSION_RESULT. tamper-regression=$TAMPER_REGRESSION_RESULT Supported Node.js range from package.json engines.node: ${safe_supported_node_range} (must be a valid semver range)." >&2
            exit 1
          fi
          echo "Mobile release gate passed native accessibility and idle-profile registration checks."

  mobile-publish:
    name: Publish tested mobile builds
    if: >-
      ${{
        github.event_name != 'pull_request' &&
        inputs.android_runner_health_only != true &&
        needs.mobile-release-gate.result == 'success' &&
        github.event_name == 'workflow_dispatch' &&
        inputs.publish == true
      }}
    needs:
      - mobile-release-gate
    runs-on: ubuntu-latest
    timeout-minutes: 45
    environment:
      name: mobile-store-submission
    steps:
      - name: Check out release candidate
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.RELEASE_NODE_VERSION }}

      - name: Download iOS native smoke artifacts
        if: ${{ always() }}
        continue-on-error: true
        id: download-publish-ios-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-ios
          path: test-results/native-large-text/ios

      - name: Clear partial iOS native smoke artifacts before publishing retry
        if: ${{ always() && steps.download-publish-ios-native-smoke.outcome != 'success' }}
        continue-on-error: true
        id: cleanup-publish-ios-native-smoke
        run: rm -rf -- test-results/native-large-text/ios && mkdir -p -- test-results/native-large-text/ios

      - name: Retry iOS native smoke artifact download for publishing
        if: ${{ always() && steps.download-publish-ios-native-smoke.outcome != 'success' && steps.cleanup-publish-ios-native-smoke.outcome == 'success' }}
        continue-on-error: true
        id: retry-publish-ios-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-ios
          path: test-results/native-large-text/ios

      - name: Download Android native smoke artifacts
        if: ${{ always() }}
        continue-on-error: true
        id: download-publish-android-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-android
          path: test-results/native-large-text/android

      - name: Clear partial Android native smoke artifacts before publishing retry
        if: ${{ always() && steps.download-publish-android-native-smoke.outcome != 'success' }}
        continue-on-error: true
        id: cleanup-publish-android-native-smoke
        run: rm -rf -- test-results/native-large-text/android && mkdir -p -- test-results/native-large-text/android

      - name: Retry Android native smoke artifact download for publishing
        if: ${{ always() && steps.download-publish-android-native-smoke.outcome != 'success' && steps.cleanup-publish-android-native-smoke.outcome == 'success' }}
        continue-on-error: true
        id: retry-publish-android-native-smoke
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: native-large-text-android
          path: test-results/native-large-text/android

      - name: Run native large-text evidence privacy and submission-boundary regression
        id: native-evidence-privacy
        run: bash scripts/run-untrusted-checker.sh pnpm run test:native-large-text-evidence

      - name: Summarize native evidence privacy regression
        if: ${{ always() }}
        env:
          PRIVACY_RESULT: ${{ steps.native-evidence-privacy.outcome }}
        run: |
          set -euo pipefail
          {
            echo "## Native evidence privacy and submission-boundary regression"
            echo
            if [[ "$PRIVACY_RESULT" == "success" ]]; then
              echo "- Status: **PASS**"
              echo "- Result: privacy and submission-boundary checks passed."
            else
              echo "- Status: **BLOCKED**"
              echo "- Result: privacy checks failed; store submission is blocked."
              echo '- Details: Review the "Run native large-text evidence privacy and submission-boundary regression" step log for checker diagnostics.'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Attach candidate-bound human approvals
        if: ${{ steps.native-evidence-privacy.outcome == 'success' }}
        env:
          IOS_REVIEW_BUILD_ID: ${{ inputs.ios_review_candidate_build_id }}
          IOS_SUBMIT_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          ANDROID_REVIEW_BUILD_ID: ${{ inputs.android_review_candidate_build_id }}
          ANDROID_SUBMIT_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
          GH_RUN_ID: ${{ github.run_id }}
          GH_API_URL: ${{ github.api_url }}
        run: |
          set -euo pipefail

          reviewer="$(
            curl --fail --silent --show-error \
              --header "Accept: application/vnd.github+json" \
              --header "Authorization: Bearer $GH_TOKEN" \
              --header "X-GitHub-Api-Version: 2022-11-28" \
              "$GH_API_URL/repos/$GH_REPOSITORY/actions/runs/$GH_RUN_ID/approvals" |
              node -e '
                let input = "";
                process.stdin.setEncoding("utf8");
                process.stdin.on("data", (chunk) => { input += chunk; });
                process.stdin.on("end", () => {
                  const reviews = JSON.parse(input);
                  const approval = [...reviews].reverse().find(
                    (review) =>
                      review.state === "approved" &&
                      review.environments?.some(
                        (environment) =>
                          environment.name === "mobile-store-submission",
                      ),
                  );
                  if (!approval?.user?.login) process.exit(1);
                  process.stdout.write(approval.user.login);
                });
              '
          )"
          if [[ -z "$reviewer" || "$reviewer" == *$'\n'* || "$reviewer" == *$'\r'* ]]; then
            echo "Could not verify the reviewer who approved the mobile-store-submission environment." >&2
            exit 1
          fi

          attach_approval() {
            local platform="$1"
            local approved_build_id="$2"
            local submit_build_id="$3"
            local platform_dir="test-results/native-large-text/$platform"
            local run_dirs=()
            local run_dir
            local tested_build_id
            local reviewed_at

            if [[ -z "$approved_build_id" ]]; then
              echo "Store submission requires the ${platform} review candidate build ID." >&2
              exit 1
            fi
            if [[ "$approved_build_id" == *$'\n'* || "$approved_build_id" == *$'\r'* ]]; then
              echo "The ${platform} review candidate build ID must contain exactly one line." >&2
              exit 1
            fi
            mapfile -t run_dirs < <(find "$platform_dir" -mindepth 1 -maxdepth 1 -type d -print | sort)
            if ((${#run_dirs[@]} != 1)); then
              echo "Expected exactly one ${platform} evidence run, found ${#run_dirs[@]}." >&2
              exit 1
            fi
            run_dir="${run_dirs[0]}"
            if ! awk '
              {
                sub(/\r$/, "")
                line_count++
                if ($0 !~ /^[[:space:]]*$/) non_empty_count++
              }
              END {
                exit !(line_count == 1 && non_empty_count == 1)
              }
            ' "$run_dir/candidate-build-id.txt"; then
              echo "The ${platform} downloaded candidate build ID must contain exactly one non-empty identifier line." >&2
              exit 1
            fi
            tested_build_id="$(head -n 1 "$run_dir/candidate-build-id.txt" | tr -d '\r' | sed 's/^[[:space:]]*//; s/[[:space:]]*$//')"
            if [[ "$approved_build_id" != "$tested_build_id" ]]; then
              echo "The ${platform} approval is for a different candidate build than the downloaded evidence." >&2
              exit 1
            fi
            if [[ "$submit_build_id" != "$tested_build_id" ]]; then
              echo "The ${platform} build configured for store submission does not match the downloaded and approved evidence." >&2
              exit 1
            fi
            reviewed_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            {
              printf 'platform=%s\n' "$platform"
              printf 'reviewer=%s\n' "$reviewer"
              printf 'reviewed_at_utc=%s\n' "$reviewed_at"
              printf 'candidate_build_id=%s\n' "$approved_build_id"
              printf 'decision=APPROVED\n'
              printf 'approval_scope=candidate\n'
              printf 'notes=Approval supplied to the store submission workflow.\n'
            } > "$run_dir/review-record.txt"
          }

          attach_approval ios "$IOS_REVIEW_BUILD_ID" "$IOS_SUBMIT_BUILD_ID"
          attach_approval android "$ANDROID_REVIEW_BUILD_ID" "$ANDROID_SUBMIT_BUILD_ID"

      - name: Require approved iOS and Android evidence
        if: ${{ steps.native-evidence-privacy.outcome == 'success' }}
        env:
          REVIEWED_REF: ${{ github.ref }}
          NATIVE_EVIDENCE_REQUIRE_APPROVAL: "1"
          NATIVE_IOS_EVIDENCE_ARTIFACT_URL: ${{ needs.mobile-release-gate.outputs.ios_native_evidence_artifact_url }}
          NATIVE_ANDROID_EVIDENCE_ARTIFACT_URL: ${{ needs.mobile-release-gate.outputs.android_native_evidence_artifact_url }}
          NATIVE_IOS_EVIDENCE_DIGEST_MANIFEST: ${{ needs.mobile-release-gate.outputs.ios_native_evidence_digest_manifest }}
          NATIVE_ANDROID_EVIDENCE_DIGEST_MANIFEST: ${{ needs.mobile-release-gate.outputs.android_native_evidence_digest_manifest }}
          NATIVE_IOS_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.download-publish-ios-native-smoke.outcome == 'success' && 'success' || steps.retry-publish-ios-native-smoke.outcome }}
          NATIVE_ANDROID_EVIDENCE_DOWNLOAD_RESULT: ${{ steps.download-publish-android-native-smoke.outcome == 'success' && 'success' || steps.retry-publish-android-native-smoke.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          source scripts/workflow-output-safety.sh
          resolved_commit_sha="$(git rev-parse --verify HEAD)"
          safe_reviewed_ref="$(sanitize_workflow_text "$REVIEWED_REF")"

          {
            echo "## Reviewed release revision"
            printf -- '- Checked ref: `%s`\n' "$safe_reviewed_ref"
            printf -- '- Resolved commit SHA: `%s`\n' "$resolved_commit_sha"
          } >> "$GITHUB_STEP_SUMMARY"

          bash scripts/run-untrusted-checker.sh bash scripts/check-native-large-text-evidence.sh

      - name: Verify publishing inputs
        if: ${{ steps.native-evidence-privacy.outcome == 'success' }}
        env:
          EAS_TOKEN: ${{ secrets.EAS_TOKEN }}
          IOS_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          ANDROID_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
        run: |
          set -euo pipefail
          : "${EAS_TOKEN:?Set EAS_TOKEN in the protected mobile-store-submission environment.}"
          : "${IOS_BUILD_ID:?Set the NATIVE_SMOKE_IOS_BUILD_ID repository variable.}"
          : "${ANDROID_BUILD_ID:?Set the NATIVE_SMOKE_ANDROID_BUILD_ID repository variable.}"

      - name: Submit the tested iOS and Android candidates
        if: ${{ steps.native-evidence-privacy.outcome == 'success' }}
        env:
          EXPO_TOKEN: ${{ secrets.EAS_TOKEN }}
          IOS_BUILD_ID: ${{ env.NATIVE_SMOKE_IOS_BUILD_ID }}
          ANDROID_BUILD_ID: ${{ env.NATIVE_SMOKE_ANDROID_BUILD_ID }}
        run: |
          set -euo pipefail
          pnpm dlx eas-cli@${{ env.EAS_CLI_VERSION }} submit \
            --platform ios \
            --id "$IOS_BUILD_ID" \
            --non-interactive
          pnpm dlx eas-cli@${{ env.EAS_CLI_VERSION }} submit \
            --platform android \
            --id "$ANDROID_BUILD_ID" \
            --non-interactive
