## `.expo` directory

The `.expo` folder is created when an Expo project is started using `expo start`.

- `devices.json` contains information about devices that have recently opened this project.
- `settings.json` contains the server configuration used to serve the application manifest.

Do not commit or share the `.expo` folder. It is machine-specific and is already ignored by the repository `.gitignore`.
